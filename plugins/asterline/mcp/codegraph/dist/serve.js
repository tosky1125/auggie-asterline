#!/usr/bin/env node
import{join as join3,resolve as resolve5}from"node:path";import{cwd,env,stdin,stderr,stdout}from"node:process";import{fileURLToPath as fileURLToPath2}from"node:url";import{spawn}from"node:child_process";function isPlainRecord(value){return typeof value==="object"&&value!==null&&!Array.isArray(value)}function successResponse(id,result){return{jsonrpc:"2.0",id,result}}function errorResponse(id,code,message,data){return{jsonrpc:"2.0",id,error:data===undefined?{code,message}:{code,message,data}}}function jsonRpcId(value){return typeof value==="string"||typeof value==="number"||value===null?value:null}var HEADER_SEPARATOR=Buffer.from(`\r
\r
`);async function*readStdioJsonRpcMessages(input){let buffer=Buffer.alloc(0);for await(const chunk of input){buffer=Buffer.concat([buffer,bufferFromChunk(chunk)]);while(true){const result=readNextMessage(buffer);if(result.kind==="incomplete")break;buffer=result.remaining;if(result.message)yield result.message}}const trailing=buffer.toString("utf8").trim();if(trailing.length>0){yield parseJsonPayload(trailing,"line")}}function writeStdioJsonRpcResponse(output,response,responseMode){const body=JSON.stringify(response);if(responseMode==="framed"){output.write(`Content-Length: ${Buffer.byteLength(body,"utf8")}\r
\r
${body}`);return}output.write(`${body}
`)}function readNextMessage(buffer){if(buffer.length===0)return{kind:"incomplete"};return startsWithContentLength(buffer)?readFramedMessage(buffer):readLineMessage(buffer)}function readLineMessage(buffer){const newlineIndex=buffer.indexOf(10);if(newlineIndex===-1)return{kind:"incomplete"};const line=buffer.subarray(0,newlineIndex).toString("utf8").replace(/\r$/,"");if(line.trim().length===0){return{kind:"complete",remaining:buffer.subarray(newlineIndex+1)}}return{kind:"complete",message:parseJsonPayload(line,"line"),remaining:buffer.subarray(newlineIndex+1)}}function readFramedMessage(buffer){const separatorIndex=buffer.indexOf(HEADER_SEPARATOR);if(separatorIndex===-1)return{kind:"incomplete"};const headers=buffer.subarray(0,separatorIndex).toString("ascii");const contentLength=parseContentLength(headers);const bodyStart=separatorIndex+HEADER_SEPARATOR.length;if(contentLength===undefined){return{kind:"complete",message:{kind:"parse_error",message:"Missing or invalid Content-Length header",responseMode:"framed"},remaining:buffer.subarray(bodyStart)}}const bodyEnd=bodyStart+contentLength;if(buffer.length<bodyEnd)return{kind:"incomplete"};const body=buffer.subarray(bodyStart,bodyEnd).toString("utf8");return{kind:"complete",message:parseJsonPayload(body,"framed"),remaining:buffer.subarray(bodyEnd)}}function startsWithContentLength(buffer){const prefix=buffer.subarray(0,"content-length:".length).toString("ascii").toLowerCase();return prefix==="content-length:"}function parseContentLength(headers){for(const line of headers.split(`\r
`)){const match=/^content-length:\s*(\d+)$/i.exec(line);if(match===null)continue;const value=match[1];if(value===undefined)return;return Number(value)}return}function parseJsonPayload(payload,responseMode){try{return{kind:"request",payload:JSON.parse(payload),responseMode}}catch(error){return{kind:"parse_error",message:error instanceof Error?error.message:String(error),responseMode}}}function bufferFromChunk(chunk){if(Buffer.isBuffer(chunk))return chunk;if(typeof chunk==="string")return Buffer.from(chunk);throw new TypeError(`Unsupported stdio chunk type: ${typeof chunk}`)}var DEFAULT_IDLE_TIMEOUT_MS=10*60000;var noopLog=()=>{};async function runJsonRpcStdioServer(config){const log=config.log??noopLog;const idleTimeoutMs=config.idleTimeoutMs??DEFAULT_IDLE_TIMEOUT_MS;const idleTimer=createIdleTimer(idleTimeoutMs,log,config.onIdleTimeout);log("stdio_started",{cwd:process.cwd(),idle_timeout_ms:idleTimeoutMs});idleTimer.arm();try{for await(const message of readStdioJsonRpcMessages(config.input)){if(idleTimer.closed())break;idleTimer.arm();if(message.kind==="parse_error"){handleParseError(message,config,log);continue}await handleRequest(message,config,log)}}finally{idleTimer.clear();log("stdio_stopped")}}function handleParseError(message,config,log){log("parse_error",{message:message.message});const response=config.parseErrorResponse?.(message.message)??errorResponse(null,-32700,"Parse error",message.message);if(response!==undefined){writeStdioJsonRpcResponse(config.output,response,message.responseMode)}}async function handleRequest(message,config,log){const parsed=message.payload;const id=isPlainRecord(parsed)?jsonRpcId(parsed["id"]):null;const method=isPlainRecord(parsed)&&typeof parsed["method"]==="string"?parsed["method"]:null;log("request",{id:id===null?null:String(id),method});try{const response=await config.handler(parsed,config.handlerOptions);if(response===undefined)return;writeStdioJsonRpcResponse(config.output,response,message.responseMode);log("response",{id:String(response.id),method,is_error:response.error!==undefined})}catch(error){if(config.onHandlerError===undefined)throw error;config.onHandlerError(error)}}function createIdleTimer(idleTimeoutMs,log,onIdleTimeout){let timer=null;let isClosed=false;return{arm:()=>{if(timer!==null)clearTimeout(timer);if(idleTimeoutMs<=0)return;timer=setTimeout(()=>{isClosed=true;log("idle_timeout",{idle_timeout_ms:idleTimeoutMs});onIdleTimeout?.()},idleTimeoutMs);timer.unref()},clear:()=>{if(timer===null)return;clearTimeout(timer);timer=null},closed:()=>isClosed}}import{extname}from"node:path";import{execPath as processExecPath}from"node:process";var WINDOWS_CMD_EXTENSIONS=new Set([".bat",".cmd"]);var WINDOWS_NODE_SCRIPT_EXTENSIONS=new Set([".cjs",".js",".mjs"]);function resolveServeProcessInvocation(command,args,platform=process.platform){if(platform!=="win32")return{args:[...args],command};const extension=extname(command).toLowerCase();if(WINDOWS_NODE_SCRIPT_EXTENSIONS.has(extension)){return{args:[command,...args],command:processExecPath}}if(WINDOWS_CMD_EXTENSIONS.has(extension)){throw new Error(`Windows command shim execution is disabled: ${command}`)}return{args:[...args],command}}class CodegraphBridgeStdioError extends Error{streamName;name="CodegraphBridgeStdioError";constructor(streamName){super(`CodeGraph MCP bridge missing child ${streamName}`);this.streamName=streamName}}var CODEGRAPH_NODE_DESCRIPTION="Inspect one named symbol or file. In symbol mode, includeCode=true includes leaf-symbol source when available. Container symbols such as classes, interfaces, structs, enums, modules, and namespaces return structural outlines with member lists by design. For container source, request a specific member symbol or use file mode with symbolsOnly=false plus offset/limit.";var CODEGRAPH_NODE_INCLUDE_CODE_DESCRIPTION="Symbol mode: include leaf-symbol source when available. Container symbols such as classes, interfaces, structs, enums, modules, and namespaces intentionally return structural outlines with members; request a specific member symbol or use file mode with symbolsOnly=false plus offset/limit for source.";var CODEGRAPH_CONTAINER_OUTLINE_GUIDANCE="Container symbols intentionally return structural outlines with members. For source, request a specific member symbol or call codegraph_node in file mode with symbolsOnly=false plus offset/limit around the symbol location.";async function runBridgedCodegraphProcess(command,args,options){const invocation=resolveServeProcessInvocation(command,args);const child=spawn(invocation.command,invocation.args,{cwd:options.cwd,env:options.env,stdio:["pipe","pipe","inherit"]});const childInput=child.stdin;const childOutput=child.stdout;if(childInput===null)throw new CodegraphBridgeStdioError("stdin");if(childOutput===null)throw new CodegraphBridgeStdioError("stdout");const pendingResponses=new Map;let defaultResponseMode="framed";const childExit=new Promise((resolveExit,reject)=>{child.once("error",reject);child.once("exit",(code,signal)=>{if(code!==null){resolveExit(code);return}resolveExit(signal===null?0:1)})});const clientDone=forwardClientToCodegraph(options.input,childInput,pendingResponses,(mode)=>{defaultResponseMode=mode});const serverDone=forwardCodegraphToClient(childOutput,options.output,pendingResponses,()=>defaultResponseMode);const bridgeDone=Promise.all([clientDone,serverDone]);const requestedShutdownMs=Number.parseInt(process.env["ASTERLINE_CODEGRAPH_SHUTDOWN_TIMEOUT_MS"]??"2000",10);const shutdownTimeoutMs=Number.isFinite(requestedShutdownMs)?Math.max(50,Math.min(requestedShutdownMs,1e4)):2000;let shutdownTimer;clientDone.then(()=>{shutdownTimer=setTimeout(()=>child.kill("SIGKILL"),shutdownTimeoutMs)});const destroyChildPipes=()=>{childInput.destroy();childOutput.destroy()};childExit.then(destroyChildPipes,destroyChildPipes);return Promise.race([childExit,bridgeDone.then(()=>childExit)]).finally(()=>{if(shutdownTimer!==undefined)clearTimeout(shutdownTimer)})}async function forwardClientToCodegraph(input,childInput,pendingResponses,setDefaultResponseMode){for await(const message of readStdioJsonRpcMessages(input)){if(message.kind==="parse_error"){continue}const responseMode=message.responseMode;setDefaultResponseMode(responseMode);const key=responseModeKey(message.payload);if(key!==null){pendingResponses.set(key,{method:jsonRpcMethod(message.payload),responseMode,toolName:jsonRpcToolName(message.payload)})}await writeLine(childInput,JSON.stringify(message.payload))}childInput.end()}async function forwardCodegraphToClient(childOutput,output,pendingResponses,defaultResponseMode){for await(const message of readStdioJsonRpcMessages(childOutput)){if(message.kind==="parse_error"){writeStdioJsonRpcResponse(output,errorResponse(null,-32700,"Parse error",message.message),defaultResponseMode());continue}const key=responseModeKey(message.payload);const pendingResponse=key===null?undefined:pendingResponses.get(key);const responseMode=pendingResponse?.responseMode??defaultResponseMode();if(key!==null)pendingResponses.delete(key);writeStdioJsonRpcResponse(output,clarifyCodegraphResponse(message.payload,pendingResponse),responseMode)}}function responseModeKey(payload){if(!isPlainRecord(payload)||!("id"in payload))return null;const id=jsonRpcId(payload["id"]);return`${typeof id}:${String(id)}`}function jsonRpcMethod(payload){if(!isPlainRecord(payload))return null;const method=payload["method"];return typeof method==="string"?method:null}function jsonRpcToolName(payload){if(jsonRpcMethod(payload)!=="tools/call"||!isPlainRecord(payload))return null;const params=payload["params"];if(!isPlainRecord(params))return null;const name=params["name"];return typeof name==="string"?name:null}function clarifyCodegraphResponse(payload,pendingResponse){if(pendingResponse?.method==="tools/list")return clarifyCodegraphToolsList(payload);if(pendingResponse?.method==="tools/call"&&pendingResponse.toolName==="codegraph_node"){return clarifyCodegraphNodeCallResult(payload)}return payload}function clarifyCodegraphToolsList(payload){if(!isPlainRecord(payload))return payload;const result=payload["result"];if(!isPlainRecord(result)||!Array.isArray(result["tools"]))return payload;let changed=false;const tools=result["tools"].map((tool)=>{if(!isPlainRecord(tool)||tool["name"]!=="codegraph_node")return tool;if(!hasCodegraphNodeContractMetadata(tool))return tool;changed=true;return clarifyCodegraphNodeTool(tool)});if(!changed)return payload;return{...payload,result:{...result,tools}}}function clarifyCodegraphNodeTool(tool){const clarified={...tool,description:CODEGRAPH_NODE_DESCRIPTION};const inputSchema=tool["inputSchema"];if(isPlainRecord(inputSchema))clarified["inputSchema"]=clarifyCodegraphNodeInputSchema(inputSchema);return clarified}function hasCodegraphNodeContractMetadata(tool){if(typeof tool["description"]==="string")return true;const inputSchema=tool["inputSchema"];if(!isPlainRecord(inputSchema))return false;const properties=inputSchema["properties"];return isPlainRecord(properties)&&isPlainRecord(properties["includeCode"])}function clarifyCodegraphNodeInputSchema(inputSchema){const properties=inputSchema["properties"];if(!isPlainRecord(properties))return inputSchema;const includeCode=properties["includeCode"];if(!isPlainRecord(includeCode))return inputSchema;return{...inputSchema,properties:{...properties,includeCode:{...includeCode,description:CODEGRAPH_NODE_INCLUDE_CODE_DESCRIPTION}}}}function clarifyCodegraphNodeCallResult(payload){if(!isPlainRecord(payload))return payload;const result=payload["result"];if(!isPlainRecord(result)||!Array.isArray(result["content"]))return payload;let changed=false;const content=result["content"].map((item)=>{if(!isPlainRecord(item)||item["type"]!=="text"||typeof item["text"]!=="string")return item;const text=clarifyContainerOutlineGuidance(item["text"]);if(text===item["text"])return item;changed=true;return{...item,text}});if(!changed)return payload;return{...payload,result:{...result,content}}}function clarifyContainerOutlineGuidance(text){if(!text.includes("Structural outline only"))return text;return text.replace(/Structural outline only[^\n]*(?:\n[^\n]*(?:Read|read)[^\n]*)?/g,CODEGRAPH_CONTAINER_OUTLINE_GUIDANCE)}async function writeLine(output,line){if(output.write(`${line}
`))return;await new Promise((resolveDrain,reject)=>{output.once("drain",resolveDrain);output.once("error",reject)})}async function runUnavailableCodegraphMcpServer(options){await runJsonRpcStdioServer({handler:handleUnavailableCodegraphMcpRequest,handlerOptions:{reason:options.reason.trim(),serverVersion:options.serverVersion},input:options.input,output:options.output})}async function handleUnavailableCodegraphMcpRequest(input,options){if(!isPlainRecord(input)){return errorResponse(null,-32600,"Invalid Request")}const id=jsonRpcId(input["id"]);const method=input["method"];if(method==="notifications/initialized")return;if(method==="ping")return successResponse(id,{});if(method==="initialize"){return successResponse(id,{capabilities:{tools:{listChanged:false}},protocolVersion:requestedProtocolVersion(input["params"]),serverInfo:{name:"codegraph",version:options.serverVersion}})}if(method==="tools/list"){return successResponse(id,{tools:[]})}if(method==="tools/call"){return successResponse(id,{content:[{text:options.reason,type:"text"}],isError:true})}return errorResponse(id,-32601,`Method not found: ${String(method)}`)}function requestedProtocolVersion(params){if(!isPlainRecord(params)||typeof params["protocolVersion"]!=="string")return"2024-11-05";return params["protocolVersion"]}var AMBIENT=["APPDATA","CI","ComSpec","HOME","HOMEDRIVE","HOMEPATH","LANG","LC_ALL","LC_CTYPE","LOCALAPPDATA","PATH","PATHEXT","Path","SystemRoot","TEMP","TMP","TMPDIR","USERPROFILE","WINDIR","XDG_CACHE_HOME","XDG_CONFIG_HOME","XDG_DATA_HOME","XDG_STATE_HOME"];function codegraphEnvironment(env,installDir){const output={};for(const key of AMBIENT){const value=env[key];if(value!==undefined)output[key]=value}return{...output,CODEGRAPH_INSTALL_DIR:installDir,CODEGRAPH_NO_DAEMON:"1",CODEGRAPH_NO_DOWNLOAD:"1",CODEGRAPH_TELEMETRY:"0",DO_NOT_TRACK:"1"}}import{existsSync,readFileSync,statSync}from"node:fs";import{homedir}from"node:os";import{isAbsolute as isAbsolute4,join as join2,resolve as resolve4}from"node:path";import{fileURLToPath}from"node:url";import{createHash as createHash2,randomUUID}from"node:crypto";import{mkdir as mkdir3,rename,rm}from"node:fs/promises";import{basename,dirname as dirname3,isAbsolute as isAbsolute3,join,resolve as resolve3}from"node:path";import{mkdir,writeFile}from"node:fs/promises";import{dirname,isAbsolute,relative,resolve}from"node:path";import{gunzipSync,inflateRawSync}from"node:zlib";var MAX_EXPANDED_BYTES=256*1024*1024;var MAX_ENTRY_BYTES=128*1024*1024;import{spawn as spawn2}from"node:child_process";import{extname as extname2}from"node:path";var MAX_STREAM_BYTES=64*1024;var MAX_COMBINED_BYTES=96*1024;var KILL_GRACE_MS=250;function buildProbeInvocation(options){const extension=extname2(options.executablePath).toLowerCase();if(options.platform==="win32"&&[".bat",".cmd"].includes(extension)){throw new Error(`Windows command shim execution is disabled: ${options.executablePath}`)}if([".cjs",".js",".mjs"].includes(extension)){return{command:options.execPath??process.execPath,args:[options.executablePath,...options.args]}}return{command:options.executablePath,args:options.args}}function probeExecutable(options){return new Promise((resolveProbe)=>{const invocation=buildProbeInvocation({executablePath:options.executablePath,args:options.probe.args,platform:options.platform,comspec:options.comspec,execPath:options.execPath});const spawnImpl=options.spawnImpl??spawn2;const child=spawnImpl(invocation.command,invocation.args,{shell:false,stdio:["ignore","pipe","pipe"]});const stdoutChunks=[];const stderrChunks=[];let stdoutBytes=0;let stderrBytes=0;let forcedError;let settled=false;let killTimer;const cleanup=()=>{clearTimeout(timeoutTimer);if(killTimer!==undefined)clearTimeout(killTimer);child.stdout.removeListener("data",onStdout);child.stderr.removeListener("data",onStderr);child.removeListener("error",onError);child.removeListener("close",onClose)};const finish=(result)=>{if(settled)return;settled=true;cleanup();resolveProbe(result)};const abort=(reason)=>{if(forcedError!==undefined)return;forcedError=reason;child.stdout.pause();child.stderr.pause();child.kill("SIGKILL");killTimer=setTimeout(()=>finish({ok:false,error:forcedError}),KILL_GRACE_MS)};const collect=(stream,chunk)=>{const bytes=Buffer.isBuffer(chunk)?chunk:Buffer.from(chunk);const streamBytes=stream==="stdout"?stdoutBytes:stderrBytes;if(streamBytes+bytes.length>MAX_STREAM_BYTES||stdoutBytes+stderrBytes+bytes.length>MAX_COMBINED_BYTES){abort(`probe output limit exceeded on ${stream}`);return}if(stream==="stdout"){stdoutBytes+=bytes.length;stdoutChunks.push(bytes)}else{stderrBytes+=bytes.length;stderrChunks.push(bytes)}};const onStdout=(chunk)=>collect("stdout",chunk);const onStderr=(chunk)=>collect("stderr",chunk);const onError=(error)=>finish({ok:false,error:error.message});const onClose=(exitCode,signal)=>{if(forcedError!==undefined){finish({ok:false,error:forcedError});return}const stdout=Buffer.concat(stdoutChunks,stdoutBytes).toString("utf8");const stderr=Buffer.concat(stderrChunks,stderrBytes).toString("utf8");const ok=signal===null&&exitCode===options.probe.expectedExit&&stdout===options.probe.expectedStdout;finish(ok?{ok:true,exitCode,stdout,stderr}:{ok:false,error:`probe mismatch: exit=${exitCode} signal=${signal??"none"} stdout=${JSON.stringify(stdout)}`})};child.stdout.on("data",onStdout);child.stderr.on("data",onStderr);child.once("error",onError);child.once("close",onClose);const timeoutTimer=setTimeout(()=>abort(`probe timed out after ${options.probe.timeoutMs}ms`),options.probe.timeoutMs)})}import{createHash}from"node:crypto";import{createReadStream}from"node:fs";import{chmod,lstat,mkdir as mkdir2,realpath}from"node:fs/promises";import{dirname as dirname2,extname as extname3,isAbsolute as isAbsolute2,relative as relative2,resolve as resolve2,sep}from"node:path";var COMMAND_SHIMS=new Set([".bat",".cmd"]);function unavailable(code,message){return{status:"unavailable",code,message}}function hasCode(error,code){return error instanceof Error&&"code"in error&&error.code===code}function contained(root,candidate){const displacement=relative2(root,candidate);return displacement===""||!displacement.startsWith(`..${sep}`)&&displacement!==".."&&!isAbsolute2(displacement)}async function sha256File(path){const hash=createHash("sha256");for await(const chunk of createReadStream(path))hash.update(chunk);return hash.digest("hex")}async function verifyNativeExecutable(options){const cacheRoot=resolve2(options.cacheRoot);const executablePath=resolve2(options.executablePath);if(!contained(cacheRoot,executablePath))return unavailable("unsafe_cache_path",`native executable escapes cache root: ${executablePath}`);if(options.platform==="win32"&&COMMAND_SHIMS.has(extname3(executablePath).toLowerCase())){return unavailable("unsafe_command_shim",`Windows command shim execution is disabled: ${executablePath}`)}try{const rootInfo=await lstat(cacheRoot);if(rootInfo.isSymbolicLink()||!rootInfo.isDirectory())return unavailable("unsafe_cache_path",`native cache root is not a real directory: ${cacheRoot}`);let cursor=cacheRoot;for(const segment of relative2(cacheRoot,executablePath).split(sep)){cursor=resolve2(cursor,segment);const info2=await lstat(cursor);if(info2.isSymbolicLink())return unavailable("unsafe_cache_path",`native cache path contains a symbolic link: ${cursor}`)}const rootRealpath=await realpath(cacheRoot);const executableRealpath=await realpath(executablePath);if(!contained(rootRealpath,executableRealpath))return unavailable("unsafe_cache_path",`native executable realpath escapes cache root: ${executableRealpath}`);const info=await lstat(executablePath);if(!info.isFile())return unavailable("unsafe_cache_path",`native executable is not a regular file: ${executablePath}`);const actualSha256=await sha256File(executablePath);if(actualSha256!==options.expectedSha256){return unavailable("integrity_mismatch",`native executable checksum mismatch: expected ${options.expectedSha256}, got ${actualSha256}`)}return{status:"verified",executablePath}}catch(error){if(hasCode(error,"ENOENT"))return unavailable("missing_or_unhealthy",`native executable is missing: ${executablePath}`);if(error instanceof Error)return unavailable("verification_failed",error.message);throw error}}var MAX_ARCHIVE_BYTES=128*1024*1024;var SHA256=/^[0-9a-f]{64}$/;var SLUG=/^(darwin|linux|win32)-(arm64|x64)$/;class NativeManifestError extends Error{constructor(message){super(message);this.name="NativeManifestError"}}function record2(value,path,fields){if(value===null||typeof value!=="object"||Array.isArray(value))throw new NativeManifestError(`${path} must be an object`);const allowed=fields.map((field)=>field.replace(/\?$/,""));for(const key of Object.keys(value))if(!allowed.includes(key))throw new NativeManifestError(`${path} has unknown field ${key}`);for(const field of fields)if(!field.endsWith("?")&&!(field in value))throw new NativeManifestError(`${path} is missing ${field}`);return value}function text(value,path,pattern){if(typeof value!=="string"||value.length===0||pattern!==undefined&&!pattern.test(value)){throw new NativeManifestError(`${path} is invalid`)}return value}function httpsUrl(value,path){const raw=text(value,path);let parsed;try{parsed=new URL(raw)}catch(error){if(error instanceof TypeError)throw new NativeManifestError(`${path} must be an HTTPS URL`);throw error}if(parsed.protocol!=="https:")throw new NativeManifestError(`${path} must be an HTTPS URL`);return raw}function safeRelativePath(value,path){const raw=text(value,path);if(raw.includes("\\")||raw.includes("\x00")||isAbsolute3(raw)||/^[A-Za-z]:/.test(raw))throw new NativeManifestError(`${path} is unsafe`);const parts=raw.split("/");if(parts.some((part)=>part===""||part==="."||part===".."))throw new NativeManifestError(`${path} is unsafe`);return raw}function parseAsset(value,path){const raw=record2(value,path,["archive","executable","executableSha256","sha256","url"]);if(!["tar.gz","tgz","zip"].includes(raw.archive))throw new NativeManifestError(`${path}.archive is invalid`);safeRelativePath(raw.executable,`${path}.executable`);text(raw.executableSha256,`${path}.executableSha256`,SHA256);text(raw.sha256,`${path}.sha256`,SHA256);httpsUrl(raw.url,`${path}.url`);return raw}function parseComponent(value,index){const path=`components[${index}]`;const raw=record2(value,path,["id","version","license","source","probe","assets"]);text(raw.id,`${path}.id`,/^[a-z0-9][a-z0-9-]*$/);text(raw.version,`${path}.version`);const license=record2(raw.license,`${path}.license`,["spdx","provenance"]);text(license.spdx,`${path}.license.spdx`);httpsUrl(license.provenance,`${path}.license.provenance`);const source=record2(raw.source,`${path}.source`,["repository","revision"]);httpsUrl(source.repository,`${path}.source.repository`);text(source.revision,`${path}.source.revision`,/^[0-9a-f]{40}$/);const probe=record2(raw.probe,`${path}.probe`,["args","expectedExit","expectedStdout","timeoutMs"]);if(!Array.isArray(probe.args)||probe.args.length>8||probe.args.some((arg)=>typeof arg!=="string"))throw new NativeManifestError(`${path}.probe.args is invalid`);if(!Number.isInteger(probe.expectedExit)||probe.expectedExit<0||probe.expectedExit>255)throw new NativeManifestError(`${path}.probe.expectedExit is invalid`);text(probe.expectedStdout,`${path}.probe.expectedStdout`);if(!Number.isInteger(probe.timeoutMs)||probe.timeoutMs<100||probe.timeoutMs>30000)throw new NativeManifestError(`${path}.probe.timeoutMs is invalid`);if(raw.assets===null||typeof raw.assets!=="object"||Array.isArray(raw.assets))throw new NativeManifestError(`${path}.assets must be an object`);const assets=raw.assets;const entries=Object.entries(assets);if(entries.length===0)throw new NativeManifestError(`${path}.assets must not be empty`);for(const[slug,asset]of entries){text(slug,`${path}.assets key`,SLUG);parseAsset(asset,`${path}.assets.${slug}`)}return raw}function validateNativeSbom(value){const raw=record2(value,"manifest",["$schema?","schemaVersion","components"]);if(raw.schemaVersion!==1)throw new NativeManifestError("manifest.schemaVersion must be 1");if(!Array.isArray(raw.components)||raw.components.length===0)throw new NativeManifestError("manifest.components must not be empty");const ids=new Set;for(const[index,component]of raw.components.entries()){const parsed=parseComponent(component,index);if(ids.has(parsed.id))throw new NativeManifestError(`manifest has duplicate component ${parsed.id}`);ids.add(parsed.id)}return value}function unavailable2(code,message){return{status:"unavailable",code,message}}function selectNativeAsset({sbom,toolId,platform=process.platform,arch=process.arch}){validateNativeSbom(sbom);const tool=sbom.components.find((candidate)=>candidate.id===toolId);if(tool===undefined)return unavailable2("unknown_tool",`native tool ${toolId} is not pinned`);const slug=`${platform}-${arch}`;const asset=tool.assets[slug];if(asset===undefined)return unavailable2("unsupported_platform",`${tool.id} ${tool.version} has no asset for ${slug}`);return{status:"selected",tool,asset,slug,platform}}async function nativeAssetDoctor(options){const selection=selectNativeAsset(options);if(selection.status==="unavailable")return selection;const executablePath=join(options.cacheRoot,selection.tool.id,selection.tool.version,selection.slug,selection.asset.executable);const verification=await verifyNativeExecutable({cacheRoot:options.cacheRoot,executablePath,expectedSha256:selection.asset.executableSha256,platform:selection.platform});if(verification.status==="unavailable")return verification;const probe=await probeExecutable({executablePath,probe:selection.tool.probe,platform:selection.platform});if(!probe.ok)return unavailable2("missing_or_unhealthy",`${selection.tool.id} ${selection.tool.version} is unavailable: ${probe.error}`);return{status:"available",executablePath,probe}}var VERSION="1.0.1";class CodegraphPathError extends Error{constructor(message){super(message);this.name="CodegraphPathError"}}function pluginRoot(moduleUrl){return resolve4(fileURLToPath(new URL("../../../",moduleUrl)))}function pluginData(env){const configured=env["ASTERLINE_PLUGIN_DATA"]?.trim()||env["PLUGIN_DATA"]?.trim();if(configured!==undefined){if(!isAbsolute4(configured))throw new CodegraphPathError("plugin data root must be absolute");return resolve4(configured)}return join2(resolve4(env["HOME"]?.trim()||homedir()),".augment","asterline","plugin-data")}async function resolveCodegraphBinary(options){const platform=options.platform??process.platform;const arch=options.arch??process.arch;let manifest;try{manifest=JSON.parse(readFileSync(join2(options.pluginRoot,"native","SBOM.json"),"utf8"))}catch(error){return{reason:`CodeGraph MCP unavailable: native manifest could not be read (${message(error)}).`}}const component=codegraphComponent(manifest);if(component?.version!==VERSION){return{reason:`CodeGraph MCP unavailable: checksum-pinned CodeGraph ${VERSION} is absent from the native manifest.`}}try{const result=await nativeAssetDoctor({sbom:manifest,toolId:"codegraph",cacheRoot:join2(pluginData(options.env),"native"),platform,arch});if(result.status==="available")return{executablePath:result.executablePath};return{reason:`CodeGraph MCP unavailable: ${result.message}.`}}catch(error){return{reason:`CodeGraph MCP unavailable: native verification failed (${message(error)}).`}}}function resolveProjectRoot(env,fallback){for(const key of["ASTERLINE_CODEGRAPH_PROJECT_CWD","PWD"]){const value=env[key]?.trim();if(value===undefined||value.length===0)continue;const candidate=resolve4(value);if(existsSync(candidate)&&statSync(candidate).isDirectory())return candidate}return resolve4(fallback)}function codegraphComponent(value){if(!record3(value)||!Array.isArray(value["components"]))return;const raw=value["components"].find((entry)=>record3(entry)&&entry["id"]==="codegraph");if(!record3(raw)||typeof raw["version"]!=="string")return;return{version:raw["version"]}}function record3(value){return value!==null&&typeof value==="object"&&!Array.isArray(value)}function message(error){return error instanceof Error?error.message:String(error)}var VERSION2="1.0.1";async function runCodegraphMcp(){const root=pluginRoot(import.meta.url);const resolution=await resolveCodegraphBinary({env,pluginRoot:root});if(resolution.executablePath===undefined){const reason=`${resolution.reason??"CodeGraph MCP unavailable."}
`;stderr.write(reason);await runUnavailableCodegraphMcpServer({input:stdin,output:stdout,reason,serverVersion:VERSION2});return 0}return runBridgedCodegraphProcess(resolution.executablePath,["serve","--mcp"],{cwd:resolveProjectRoot(env,cwd()),env:codegraphEnvironment(env,join3(pluginData(env),"codegraph")),input:stdin,output:stdout,stderr,stdio:"pipe"})}function help(){return`Usage: asterline-codegraph-mcp
Serves the checksum-pinned CodeGraph 1.0.1 MCP runtime.
`}async function main(){if(process.argv.includes("--help")||process.argv.includes("-h")){stdout.write(help());return}process.exitCode=await runCodegraphMcp()}if(resolve5(process.argv[1]??"")===fileURLToPath2(import.meta.url)){main().catch((error)=>{stderr.write(`${error instanceof Error?error.message:String(error)}
`);process.exitCode=1})}export{runCodegraphMcp,help};
