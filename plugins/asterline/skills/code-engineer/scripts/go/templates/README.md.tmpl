# $name

Bootstrapped with the `code-engineer` skill's Go scaffold.

## Run

```bash
task            # fmt + lint + test
task run        # build + run server
```

See `AGENTS.md` for architecture conventions.
