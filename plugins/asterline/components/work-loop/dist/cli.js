#!/usr/bin/env node
import{readFile as readFile5}from"node:fs/promises";import{existsSync as existsSync2,lstatSync,realpathSync}from"node:fs";import{readFile as readFile3}from"node:fs/promises";import{resolve as resolve4}from"node:path";var WORK_LOOP_DIR=".asterline/work-loop";var WORK_LOOP_BRIEF="brief.md";var WORK_LOOP_GOALS="goals.json";var WORK_LOOP_LEDGER="ledger.jsonl";var INSTALLED_WORK_LOOP_COMMAND='node "$HOME/.augment/plugins/marketplaces/auggie-asterline/plugins/asterline/components/work-loop/dist/cli.js" work-loop';var WORK_LOOP_STEERING_MUTATION_KINDS=["add_subgoal","split_subgoal","reorder_pending","revise_pending_wording","revise_criterion","annotate_ledger","mark_blocked_superseded"];var WORK_LOOP_SUCCESS_CRITERION_USER_MODELS=["happy","edge","regression","adversarial"];import{existsSync}from"node:fs";import{readFile}from"node:fs/promises";import{resolve}from"node:path";class HostGoalSnapshotError extends Error{}function safeObject(value){return value&&typeof value==="object"&&!Array.isArray(value)?value:{}}function safeString(value){return typeof value==="string"?value.trim():""}function normalizeStatus(value){const status=safeString(value).toLowerCase();if(status==="complete"||status==="completed"||status==="done")return"complete";if(status==="cancelled"||status==="canceled")return"cancelled";if(status==="failed"||status==="failure")return"failed";if(status==="active"||status==="in_progress"||status==="pending"||status==="running")return"active";return"unknown"}function normalizeObjective(value){return value.replace(/\s+/g," ").trim()}function parseHostGoalSnapshot(value){const root=safeObject(value);const goalValue=Object.hasOwn(root,"goal")?root["goal"]:value;if(goalValue===null||goalValue===undefined||goalValue===false){return{available:false,raw:value}}const goal=safeObject(goalValue);const objective=safeString(goal["objective"]??goal["goal"]??goal["description"]??root["objective"]);const status=normalizeStatus(goal["status"]??root["status"]);return{available:Boolean(objective||status!=="unknown"),...objective?{objective}:{},status,raw:value}}async function readHostGoalSnapshotInput(raw,cwd=process.cwd()){if(!raw?.trim())return null;const trimmed=raw.trim();try{return parseHostGoalSnapshot(JSON.parse(trimmed))}catch{const path=resolve(cwd,trimmed);if(!existsSync(path)){throw new HostGoalSnapshotError(`host goal snapshot is neither valid JSON nor a readable path: ${trimmed}`)}try{return parseHostGoalSnapshot(JSON.parse(await readFile(path,"utf-8")))}catch(error){throw new HostGoalSnapshotError(`host goal snapshot path does not contain valid JSON: ${trimmed}${error instanceof Error?` (${error.message})`:""}`)}}}function reconcileHostGoalSnapshot(snapshot,options){const effectiveSnapshot=snapshot??{available:false,raw:null};const errors=[];const warnings=[];if(!effectiveSnapshot.available){const message="host goal snapshot is absent or reports no active goal; call get_goal and pass its JSON with --host-goal-json.";if(options.requireSnapshot)errors.push(message);else warnings.push(message);return{ok:errors.length===0,snapshot:effectiveSnapshot,warnings,errors}}const expected=normalizeObjective(options.expectedObjective);const accepted=new Set([expected,...(options.acceptedObjectives??[]).map((objective)=>normalizeObjective(objective))].filter(Boolean));const actual=normalizeObjective(effectiveSnapshot.objective??"");if(!actual){errors.push("host goal snapshot is missing objective text.")}else if(!accepted.has(actual)){errors.push(`host goal objective mismatch: expected "${expected}", got "${actual}".`)}const allowed=options.allowedStatuses??(options.requireComplete?["complete"]:["active","complete"]);const actualStatus=effectiveSnapshot.status??"unknown";if(!allowed.includes(actualStatus)){errors.push(`host goal status mismatch: expected ${allowed.join(" or ")}, got ${actualStatus}.`)}if(options.requireComplete&&actualStatus!=="complete"){errors.push('host goal is not complete; call update_goal({status: "complete"}) only after the objective is actually complete, then pass the fresh get_goal JSON.')}return{ok:errors.length===0,snapshot:effectiveSnapshot,warnings,errors}}function formatHostGoalReconciliation(reconciliation){const parts=[...reconciliation.errors,...reconciliation.warnings];return parts.join(" ")}import{lstat,mkdir}from"node:fs/promises";import{isAbsolute,join,relative,resolve as resolve2,sep}from"node:path";class WorkLoopError extends Error{code;details;constructor(message,code,opts){super(message,opts?.cause===undefined?undefined:{cause:opts.cause});this.name="WorkLoopError";this.code=code;if(opts?.details!==undefined){this.details=opts.details}}}function iso(){return new Date().toISOString()}var SESSION_ENV_KEYS=["ASTERLINE_WORK_LOOP_SESSION_ID","AUGGIE_SESSION_ID"];var SESSION_ID_PATTERN=/^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$/u;function normalizeWorkLoopSessionId(sessionId){const trimmed=sessionId?.trim();if(!trimmed)return null;if(SESSION_ID_PATTERN.test(trimmed))return trimmed;throw new WorkLoopError("Session id must be 1-128 ASCII letters, digits, dots, underscores, or dashes and must start with a letter or digit.","WORK_LOOP_SESSION_ID_INVALID",{details:{sessionId:trimmed}})}function resolveWorkLoopSessionIdFromEnv(env=process.env){for(const key of SESSION_ENV_KEYS){const normalized=normalizeWorkLoopSessionId(env[key]);if(normalized!==null)return normalized}return null}function workLoopRelativeDir(scope){const sessionId=normalizeWorkLoopSessionId(scope?.sessionId);return sessionId===null?WORK_LOOP_DIR:`${WORK_LOOP_DIR}/${sessionId}`}function workLoopDir(repoRoot,scope){const root=resolve2(repoRoot);const path=resolve2(root,workLoopRelativeDir(scope));const child=relative(root,path);if(child===""||child.startsWith("..")||isAbsolute(child)){throw new WorkLoopError("Work-loop state path escapes the repository.","WORK_LOOP_PATH_OUTSIDE_REPOSITORY")}return path}async function ensureWorkLoopDir(repoRoot,scope){const root=resolve2(repoRoot);const segments=workLoopRelativeDir(scope).split("/");let current=root;for(const segment of segments){current=join(current,segment);try{const info=await lstat(current);if(info.isSymbolicLink()||!info.isDirectory()){throw new WorkLoopError("Work-loop state path contains a symlink or non-directory.","WORK_LOOP_PATH_UNSAFE")}}catch(error){if(!hasCode(error,"ENOENT"))throw error;await mkdir(current,{mode:448})}}return current}function hasCode(error,code){return error instanceof Error&&"code"in error&&error.code===code}function workLoopBriefRelativePath(scope){return`${workLoopRelativeDir(scope)}/${WORK_LOOP_BRIEF}`}function workLoopGoalsRelativePath(scope){return`${workLoopRelativeDir(scope)}/${WORK_LOOP_GOALS}`}function workLoopLedgerRelativePath(scope){return`${workLoopRelativeDir(scope)}/${WORK_LOOP_LEDGER}`}function workLoopAttemptEvidenceDir(goalId,attempt,scope){const sessionId=normalizeWorkLoopSessionId(scope?.sessionId)??resolveWorkLoopSessionIdFromEnv()??"session";return`.asterline/evidence/work-loop/${sessionId}/${goalId}/a${attempt}`}function isWithinAttemptDir(candidate,attemptRoot){const child=relative(resolve2(attemptRoot),resolve2(candidate));return child===""||!child.startsWith(`..${sep}`)&&child!==".."&&!isAbsolute(child)}function workLoopBriefPath(repoRoot,scope){return join(workLoopDir(repoRoot,scope),WORK_LOOP_BRIEF)}function workLoopGoalsPath(repoRoot,scope){return join(workLoopDir(repoRoot,scope),WORK_LOOP_GOALS)}function workLoopLedgerPath(repoRoot,scope){return join(workLoopDir(repoRoot,scope),WORK_LOOP_LEDGER)}function repoRelative(absolutePath,repoRoot){const slashPrefix=`${repoRoot}/`;const backslashPrefix=`${repoRoot}\\`;if(absolutePath.startsWith(slashPrefix))return absolutePath.slice(slashPrefix.length).split("\\").join("/");if(absolutePath.startsWith(backslashPrefix))return absolutePath.slice(backslashPrefix.length).split("\\").join("/");return absolutePath.split("\\").join("/")}var WORK_LOOP_AGGREGATE_ASTERLINE_OBJECTIVE=aggregateAsterlineObjectiveForScope();function aggregateAsterlineObjectiveForScope(scope){return`Complete the durable work-loop plan in ${workLoopGoalsRelativePath(scope)}, including later accepted/appended stories, under the original brief constraints; use ${workLoopLedgerRelativePath(scope)} as the audit trail.`}function hostGoalMode(plan){return plan.hostGoalMode??"per_story"}function isResolvedStatus(status){return status==="complete"}function isSupersededResolved(goal,plan){if(goal.steeringStatus!=="superseded")return false;const replacements=goal.supersededBy??[];if(replacements.length===0)return false;return replacements.every((id)=>{const replacement=plan.goals.find((candidate)=>candidate.id===id);return replacement!==undefined&&isResolvedStatus(replacement.status)})}function isCompletionBlocking(goal,plan){if(goal.steeringStatus==="superseded")return!isSupersededResolved(goal,plan);if(goal.steeringStatus==="blocked")return true;return!isResolvedStatus(goal.status)}function isCompletionBlockingForFinalCandidate(candidate,finalCandidate,plan){if(candidate.id===finalCandidate.id)return false;if(candidate.steeringStatus==="superseded"){const replacements=candidate.supersededBy??[];if(replacements.length===0)return true;return!replacements.every((id)=>{if(id===finalCandidate.id)return true;const replacement=plan.goals.find((goal)=>goal.id===id);return replacement!==undefined&&isResolvedStatus(replacement.status)})}return isCompletionBlocking(candidate,plan)}function isWorkLoopDone(plan){if(plan.aggregateCompletion?.status==="complete")return true;return plan.goals.every((goal)=>!isCompletionBlocking(goal,plan))}function isFinalRunCompletionCandidate(plan,goal){return isCompletionBlocking(goal,plan)&&plan.goals.every((candidate)=>!isCompletionBlockingForFinalCandidate(candidate,goal,plan))}function aggregateAsterlineObjective(plan){return plan.asterlineObjective??WORK_LOOP_AGGREGATE_ASTERLINE_OBJECTIVE}function expectedAsterlineObjective(plan,goal){return hostGoalMode(plan)==="aggregate"?aggregateAsterlineObjective(plan):goal.objective}function compatibleAsterlineObjectives(plan){return[aggregateAsterlineObjective(plan),...plan.asterlineObjectiveAliases??[]]}function hasAllCriteriaPass(goal){return goal.successCriteria.length>0&&goal.successCriteria.every((criterion)=>criterion.status==="pass")}function isEssentialCriterion(criterion){return criterion.essential??true}function essentialCriteriaOf(goal){const explicit=goal.successCriteria.filter(isEssentialCriterion);if(explicit.length>0)return explicit;const happy=goal.successCriteria.find((criterion)=>criterion.userModel==="happy");return happy===undefined?[]:[happy]}function hasEssentialCriteriaPass(goal){const criteria=essentialCriteriaOf(goal);return criteria.length>0&&criteria.every((criterion)=>criterion.status==="pass")}import{randomUUID}from"node:crypto";import{open,readFile as readFile2,rename,rm,stat,unlink,writeFile}from"node:fs/promises";import{join as join2}from"node:path";import{createReadStream}from"node:fs";import{appendFile}from"node:fs/promises";import{createInterface}from"node:readline";function hasCode2(error,code){return error instanceof Error&&"code"in error&&error.code===code}function isSteeringKind(value){return value==="steering_accepted"||value==="steering_rejected"||value==="criteria_revised"}async function appendLedger(repoRoot,entry,scope){await ensureWorkLoopDir(repoRoot,scope);await appendFile(workLoopLedgerPath(repoRoot,scope),`${JSON.stringify(entry)}
`,"utf8")}async function*ledgerLines(repoRoot,scope){const stream=createReadStream(workLoopLedgerPath(repoRoot,scope),{encoding:"utf8"});const lines=createInterface({input:stream,crlfDelay:Number.POSITIVE_INFINITY});try{for await(const line of lines)if(line.trim().length>0)yield line}catch(error){if(!hasCode2(error,"ENOENT"))throw error}finally{lines.close();stream.destroy()}}function parseLedgerEntry(line){const value=JSON.parse(line);if(isLedgerEntry(value))return value;throw new TypeError("Invalid work-loop ledger entry.")}function isLedgerEntry(value){return typeof value==="object"&&value!==null&&!Array.isArray(value)&&"kind"in value&&"at"in value}async function findAcceptedSteeringLedgerEntry(repoRoot,key,scope){const probe=JSON.stringify(key);for await(const line of ledgerLines(repoRoot,scope)){if(!line.includes(probe))continue;const entry=parseLedgerEntry(line);if(!isSteeringKind(entry.kind)||entry.steering?.invariant.accepted!==true)continue;if(entry.idempotencyKey===key||entry.steering.idempotencyKey===key||entry.steering.promptSignature===key)return entry}return}var LEGACY_OBJECTIVE_PREFIX=`Complete all work-loop stories in ${WORK_LOOP_DIR}/${WORK_LOOP_GOALS}: `;var LEGACY_OBJECTIVE=`Complete all work-loop stories listed in ${WORK_LOOP_DIR}/${WORK_LOOP_GOALS}. Use ${WORK_LOOP_DIR}/${WORK_LOOP_LEDGER} as the durable audit trail.`;var locks=new Map;var LOCK_FILE=".mutation.lock";var LOCK_WAIT_MS=25;var LOCK_ATTEMPTS=400;var MALFORMED_LOCK_STALE_MS=30000;function hasCode3(error,code){return error instanceof Error&&"code"in error&&error.code===code}function isLegacyEnumeratedAggregateObjective(objective){return objective===LEGACY_OBJECTIVE||Boolean(objective?.startsWith(LEGACY_OBJECTIVE_PREFIX))}async function withWorkLoopMutationLock(repoRoot,scopeOrFn,maybeFn){const scope=typeof scopeOrFn==="function"?undefined:scopeOrFn;const fn=typeof scopeOrFn==="function"?scopeOrFn:maybeFn;if(fn===undefined)throw new WorkLoopError("Missing work-loop mutation body.","WORK_LOOP_LOCK_BODY_MISSING");const lockKey=`${repoRoot}\x00${workLoopRelativeDir(scope)}`;const prior=locks.get(lockKey)??Promise.resolve(undefined);const run=prior.then(()=>withFileLock(repoRoot,scope,fn),()=>withFileLock(repoRoot,scope,fn));const gate=run.then(()=>{return},()=>{return});locks.set(lockKey,gate);gate.then(()=>{if(locks.get(lockKey)===gate)locks.delete(lockKey)});return run}async function withFileLock(repoRoot,scope,fn){const owner=await acquireFileLock(repoRoot,scope);try{return await fn()}finally{await releaseFileLock(repoRoot,scope,owner)}}async function acquireFileLock(repoRoot,scope){await ensureWorkLoopDir(repoRoot,scope);const path=join2(workLoopDir(repoRoot,scope),LOCK_FILE);const owner={pid:process.pid,token:randomUUID(),createdAt:iso()};for(let attempt=0;attempt<LOCK_ATTEMPTS;attempt+=1){try{const handle=await open(path,"wx",384);try{await handle.writeFile(`${JSON.stringify(owner)}
`,"utf8")}finally{await handle.close()}return owner}catch(error){if(!hasCode3(error,"EEXIST"))throw error;if(await recoverStaleLock(path))continue;await new Promise((resolveWait)=>setTimeout(resolveWait,LOCK_WAIT_MS))}}throw new WorkLoopError("Timed out waiting for the work-loop mutation lock.","WORK_LOOP_LOCK_TIMEOUT")}async function recoverStaleLock(path){let raw;try{raw=await readFile2(path,"utf8")}catch(error){if(hasCode3(error,"ENOENT"))return true;throw error}let pid;try{const value=JSON.parse(raw);if(isRecord(value)&&typeof value["pid"]==="number"&&Number.isSafeInteger(value["pid"]))pid=value["pid"]}catch(error){if(!(error instanceof SyntaxError))throw error}if(pid!==undefined&&processIsAlive(pid))return false;if(pid===undefined){try{const info=await stat(path);if(Date.now()-info.mtimeMs<MALFORMED_LOCK_STALE_MS)return false}catch(error){if(hasCode3(error,"ENOENT"))return true;throw error}}const quarantine=`${path}.stale-${process.pid}-${randomUUID()}`;try{await rename(path,quarantine);await rm(quarantine,{force:true});return true}catch(error){if(hasCode3(error,"ENOENT"))return true;throw error}}function processIsAlive(pid){try{process.kill(pid,0);return true}catch(error){if(hasCode3(error,"ESRCH"))return false;if(hasCode3(error,"EPERM"))return true;throw error}}async function releaseFileLock(repoRoot,scope,owner){const path=join2(workLoopDir(repoRoot,scope),LOCK_FILE);try{const value=JSON.parse(await readFile2(path,"utf8"));if(isRecord(value)&&value["token"]===owner.token)await unlink(path)}catch(error){if(error instanceof SyntaxError||hasCode3(error,"ENOENT"))return;throw error}}function isRecord(value){return typeof value==="object"&&value!==null&&!Array.isArray(value)}function isWorkLoopPlan(value){return isRecord(value)&&value["version"]===1&&Array.isArray(value["goals"])}async function readWorkLoopPlan(repoRoot,scope){await ensureWorkLoopDir(repoRoot,scope);const path=workLoopGoalsPath(repoRoot,scope);let raw;try{raw=await readFile2(path,"utf8")}catch(error){if(!hasCode3(error,"ENOENT"))throw error;throw new WorkLoopError(`No work-loop plan found at ${repoRelative(path,repoRoot)}. Run \`${INSTALLED_WORK_LOOP_COMMAND} create-goals ...\` first.`,"WORK_LOOP_PLAN_MISSING",{cause:error})}let value;try{value=JSON.parse(raw)}catch(error){if(!(error instanceof SyntaxError))throw error;throw new WorkLoopError(`Invalid JSON in work-loop plan at ${repoRelative(path,repoRoot)}.`,"WORK_LOOP_PLAN_INVALID",{cause:error})}if(!isWorkLoopPlan(value)){throw new WorkLoopError(`Invalid work-loop plan at ${repoRelative(path,repoRoot)}.`,"WORK_LOOP_PLAN_INVALID")}const parsed=value;const previousObjective=parsed.asterlineObjective;if((parsed.hostGoalMode??"per_story")==="aggregate"&&isLegacyEnumeratedAggregateObjective(previousObjective)){const now=iso();parsed.asterlineObjective=aggregateAsterlineObjectiveForScope(scope);parsed.asterlineObjectiveAliases=[...new Set([...parsed.asterlineObjectiveAliases??[],previousObjective])];parsed.updatedAt=now;await writePlan(repoRoot,parsed,scope);await appendLedger(repoRoot,{at:now,kind:"aggregate_objective_migrated",message:"Migrated legacy enumerated aggregate Asterline objective to the stable pointer objective.",before:{asterlineObjective:previousObjective},after:{asterlineObjective:parsed.asterlineObjective}},scope)}return parsed}async function writePlan(repoRoot,plan,scope){await ensureWorkLoopDir(repoRoot,scope);const path=workLoopGoalsPath(repoRoot,scope);const tmpPath=`${path}.${process.pid}.${Date.now()}.tmp`;try{await writeFile(tmpPath,`${JSON.stringify(plan,null,2)}
`,{encoding:"utf8",mode:384});await rename(tmpPath,path)}finally{await rm(tmpPath,{force:true})}}function workLoopFail(message,code,details){throw new WorkLoopError(message,code,{details})}function ledgerKind(status){switch(status){case"pass":return"evidence_captured";case"fail":return"criterion_failed";case"blocked":return"criterion_blocked";default:return workLoopFail("Invalid criterion status.","WORK_LOOP_CRITERION_STATUS_INVALID",{status})}}function findGoal(plan,goalId){const goal=plan.goals.find((candidate)=>candidate.id===goalId);return goal??workLoopFail(`WorkLoop goal not found: ${goalId}.`,"WORK_LOOP_GOAL_NOT_FOUND",{goalId})}function findCriterion(goal,criterionId){const criterion=goal.successCriteria.find((candidate)=>candidate.id===criterionId);return criterion??workLoopFail(`Success criterion not found: ${criterionId}.`,"WORK_LOOP_CRITERION_NOT_FOUND",{goalId:goal.id,criterionId})}function nonEmptyEvidence(evidence){const trimmed=evidence.trim();return trimmed||workLoopFail("Evidence must be a non-empty string.","WORK_LOOP_EVIDENCE_REQUIRED",{})}async function recordEvidence(repoRoot,args,scope){return withWorkLoopMutationLock(repoRoot,scope,async()=>{const plan=await readWorkLoopPlan(repoRoot,scope);const goal=findGoal(plan,args.goalId);const criterion=findCriterion(goal,args.criterionId);const evidence=nonEmptyEvidence(args.evidence);const kind=ledgerKind(args.status);const prevStatus=criterion.status;const capturedAt=iso();criterion.status=args.status;criterion.capturedEvidence=evidence;criterion.capturedAt=capturedAt;if(args.notes!==undefined)criterion.notes=args.notes;goal.updatedAt=capturedAt;plan.updatedAt=capturedAt;await writePlan(repoRoot,plan,scope);const ledgerEntry={at:capturedAt,kind,goalId:goal.id,criterionId:criterion.id,criterionStatus:args.status,evidence,capturedEvidence:evidence,before:{status:prevStatus},after:{goalId:goal.id,criterionId:criterion.id,status:args.status,evidence,capturedAt,prevStatus}};await appendLedger(repoRoot,ledgerEntry,scope);return{plan,goal,criterion,ledgerEntry}})}function unresolvedCriteriaOf(goal){return goal.successCriteria.filter((criterion)=>criterion.status!=="pass")}function unresolvedEssentialCriteriaOf(goal){const ids=new Set(essentialCriteriaOf(goal).map((criterion)=>criterion.id));return goal.successCriteria.filter((criterion)=>ids.has(criterion.id)&&criterion.status!=="pass")}function requireAllCriteriaPass(goal){if(hasAllCriteriaPass(goal))return;throw new WorkLoopError(`Goal ${goal.id} has unresolved success criteria.`,"WORK_LOOP_CRITERIA_NOT_ALL_PASS",{details:{goalId:goal.id,unresolved:unresolvedCriteriaOf(goal).map((criterion)=>({id:criterion.id,status:criterion.status}))}})}function requireAllPlanCriteriaPass(plan){const unresolved=plan.goals.flatMap((goal)=>unresolvedCriteriaOf(goal).map((criterion)=>({goalId:goal.id,id:criterion.id,status:criterion.status})));if(unresolved.length===0)return;throw new WorkLoopError("Work-loop aggregate has unresolved success criteria.","WORK_LOOP_CRITERIA_NOT_ALL_PASS",{details:{unresolved}})}function requireEssentialCriteriaPass(goal){if(hasEssentialCriteriaPass(goal))return;throw new WorkLoopError(`Goal ${goal.id} has unresolved essential success criteria.`,"WORK_LOOP_CRITERIA_NOT_ALL_PASS",{details:{goalId:goal.id,unresolved:unresolvedEssentialCriteriaOf(goal).map((criterion)=>({id:criterion.id,status:criterion.status}))}})}import{resolve as resolve3}from"node:path";var PLACEHOLDER_PATTERN=/^(?:placeholder|todo|tbd|n\/a|stub)$/i;function invalid(message,field){throw new WorkLoopError(message,"WORK_LOOP_QUALITY_GATE_INVALID",{details:{field}})}function isRecord2(value){return typeof value==="object"&&value!==null&&!Array.isArray(value)}function section(value,field){return isRecord2(value)?value:invalid(`Final quality gate is missing ${field} evidence.`,field)}function textField(value,field){if(typeof value!=="string"||value.trim()==="")invalid(`Final quality gate requires non-empty ${field}.`,field);const trimmed=value.trim();if(PLACEHOLDER_PATTERN.test(trimmed))invalid(`Final quality gate rejects placeholder ${field}.`,field);return trimmed}function numberField(value,field){return typeof value==="number"&&Number.isFinite(value)?value:invalid(`Final quality gate requires numeric ${field}.`,field)}function stringArray(value,field){if(!Array.isArray(value)||value.length===0)return invalid(`Final quality gate requires ${field}.`,field);return value.map((item)=>textField(item,field))}function emptyBlockers(value,field){if(Array.isArray(value)&&value.length===0)return[];invalid(`${field} must be empty.`,field)}function literal(value,expected,field){if(value===expected)return expected;invalid(`${field} must be ${String(expected)}.`,field)}function passedVerdict(value,field){return value==="passed"?"passed":invalid(`${field} must be passed.`,field)}function codeQualityStatusField(value,field){return value==="CLEAR"||value==="WATCH"?value:invalid(`${field} must be CLEAR or WATCH.`,field)}function adversarialVerdict(value,field){const row=section(value,field);if(row["verdict"]==="passed")return{verdict:"passed"};if(row["verdict"]==="not_applicable")return{verdict:"not_applicable",reason:textField(row["reason"],`${field}.reason`)};return invalid(`${field}.verdict must be passed or not_applicable.`,`${field}.verdict`)}var BLOCKER_FIELD_KEYS="blocker blockerSignature blockerEvidence blockerOccurrences blockedAt".split(" ");var URL_PATTERN=/https?:\/\/\S+/g;var PUNCTUATION_PATTERN=/[`"'()[\]{}:,;]/g;var WHITESPACE_PATTERN=/\s+/g;var AUTH_PATTERN=/\b(auth\w*|credential\w*|token|permission\w*|scope\w*|access|unauthorized|forbidden|401|403)\b/;var MISSING_PATTERN=/\b(unset|missing|required|requires|without|omit\w*|not set|not available|no read packages|read packages)\b/;var GHCR_PATTERN=/\b(ghcr|github container registry|read packages|imagepullsecret|package api|anonymous|container image)\b/;var GHCR_401_PATTERN=/\b(401|unauthorized|anonymous pull|authentication required)\b/;var GHCR_403_PATTERN=/\b(403|forbidden|read packages|package api)\b/;function normalizeBlockerEvidence(evidence){return evidence.toLowerCase().replace(URL_PATTERN," ").replace(PUNCTUATION_PATTERN," ").replace(WHITESPACE_PATTERN," ").trim()}function classifyExternalAuthorizationBlocker(evidence){const normalized=normalizeBlockerEvidence(evidence);if(!normalized||!AUTH_PATTERN.test(normalized)||!MISSING_PATTERN.test(normalized))return null;if(!GHCR_PATTERN.test(normalized))return"EXTERNAL_AUTHORIZATION_REQUIRED";const parts=[GHCR_401_PATTERN.test(normalized)?"HTTP_401_ANONYMOUS":null,GHCR_403_PATTERN.test(normalized)?"HTTP_403_NO_READ_PACKAGES":null].filter((part)=>part!==null);return`GHCR_PULL_ACCESS:${parts.join("+")||"AUTHORIZATION_REQUIRED"}:GHCR_VISIBILITY_OR_CREDENTIAL_REQUIRED`}function nestedBlockerSignature(goal){const blocker=Reflect.get(goal,"blocker");const signature=isRecord2(blocker)?blocker["signature"]:null;return typeof signature==="string"?signature:null}function sameBlockerOccurrences(plan,signature){return plan.goals.filter((goal)=>goal.blockerSignature===signature||nestedBlockerSignature(goal)===signature).length}function clearGoalBlockerFields(goal){for(const key of BLOCKER_FIELD_KEYS)Reflect.deleteProperty(goal,key)}var REVIEWER_ROLES={codeReview:"judge",manualQa:"operator",gateReview:"skeptic"};var UNCONDITIONAL_APPROVAL_PATTERN=/\bUNCONDITIONAL\s+APPROVAL\b/i;function reviewerRoleField(value,expected,field){const actual=textField(value,field);if(actual!==expected)invalid(`${field} must be ${expected}.`,field);return expected}function surfaceField(value,field){if(value==="cli"||value==="http"||value==="tmux"||value==="browser"||value==="gui"||value==="data")return value;return invalid(`${field} must be a supported manual QA surface.`,field)}function kindField(value,field){if(value==="cli-transcript"||value==="log"||value==="screenshot"||value==="image"||value==="http-dump"||value==="data-diff")return value;return invalid(`${field} must be a supported artifact kind.`,field)}function artifactCompatible(surface,kind){if(surface==="cli"||surface==="tmux")return kind==="cli-transcript"||kind==="log";if(surface==="http")return kind==="http-dump";if(surface==="browser"||surface==="gui")return kind==="screenshot"||kind==="image";return kind==="data-diff"}function checkFile(path,field,opts){if(opts===undefined)return;const absolute=resolve3(opts.repoRoot,path);if(!opts.fs.existsSync(absolute))invalid(`${field} must point to an existing artifact.`,field);const info=opts.fs.lstatSync(absolute);if(info.isSymbolicLink()||!info.isFile())invalid(`${field} must point to a regular non-symlink file.`,field);if(info.size<=0)invalid(`${field} must point to a non-empty artifact.`,field);if(opts.currentAttemptDir===undefined)return;const attempt=resolve3(opts.repoRoot,opts.currentAttemptDir);if(!isWithinAttemptDir(absolute,attempt))invalid(`${field} (${path}) must point to an artifact from the current attempt (${opts.currentAttemptDir}).`,field);const realRepo=opts.fs.realpathSync(resolve3(opts.repoRoot));const realAttempt=opts.fs.realpathSync(attempt);const realArtifact=opts.fs.realpathSync(absolute);if(!isWithinAttemptDir(realAttempt,realRepo)||!isWithinAttemptDir(realArtifact,realAttempt))invalid(`${field} (${path}) must resolve inside the current attempt (${opts.currentAttemptDir}).`,field)}function validateCriteriaCoverage(input,opts){const{totalCriteria,passCount,refs}=input;if(opts===undefined){if(passCount<totalCriteria)invalid("criteriaCoverage.passCount must cover totalCriteria.","criteriaCoverage.passCount");return}const expected=new Map;const idCounts=new Map;for(const criterion of opts.criteria)idCounts.set(criterion.criterionId,(idCounts.get(criterion.criterionId)??0)+1);for(const criterion of opts.criteria){const canonical=`${criterion.goalId}:${criterion.criterionId}`;expected.set(canonical,canonical);if(idCounts.get(criterion.criterionId)===1)expected.set(criterion.criterionId,canonical)}const expectedCount=opts.criteria.length;if(totalCriteria!==expectedCount)invalid(`criteriaCoverage.totalCriteria must equal the plan criterion count (${expectedCount}).`,"criteriaCoverage.totalCriteria");if(passCount!==expectedCount)invalid(`criteriaCoverage.passCount must equal the plan criterion count (${expectedCount}).`,"criteriaCoverage.passCount");const covered=new Set;for(const ref of refs){const canonical=expected.get(ref)??invalid(`criterionRef references unknown or ambiguous plan criterion ${ref}.`,"manualQa.criterionRef");if(covered.has(canonical))invalid(`criterionRef contains duplicate plan criterion ${ref}.`,"manualQa.criterionRef");covered.add(canonical)}if(covered.size!==expectedCount)invalid("criterionRef must cover every plan criterion exactly once.","manualQa.criterionRef")}function parseArtifactRefs(value,opts){if(!Array.isArray(value)||value.length===0)invalid("manualQa.artifactRefs must not be empty.","manualQa.artifactRefs");const refs=value.map((item,index)=>{const ref=section(item,`manualQa.artifactRefs[${index}]`);const path=textField(ref["path"],`manualQa.artifactRefs[${index}].path`);checkFile(path,`manualQa.artifactRefs[${index}].path`,opts);return{id:textField(ref["id"],`manualQa.artifactRefs[${index}].id`),kind:kindField(ref["kind"],`manualQa.artifactRefs[${index}].kind`),description:textField(ref["description"],`manualQa.artifactRefs[${index}].description`),path}});const ids=new Set;for(const ref of refs){if(ids.has(ref.id))invalid(`manualQa.artifactRefs contains duplicate ${ref.id}.`,"manualQa.artifactRefs");ids.add(ref.id)}return refs}function referencedArtifacts(value,field,byId){return stringArray(value,field).map((id)=>byId.get(id)??invalid(`${field} references unknown artifact ${id}.`,field))}function parseSurfaceEvidence(value,byId){if(!Array.isArray(value)||value.length===0)invalid("manualQa.surfaceEvidence must not be empty.","manualQa.surfaceEvidence");return value.map((item,index)=>{const field=`manualQa.surfaceEvidence[${index}]`;const row=section(item,field);const surface=surfaceField(row["surface"],`${field}.surface`);const artifacts=referencedArtifacts(row["artifactRefs"],`${field}.artifactRefs`,byId);for(const artifact of artifacts)if(!artifactCompatible(surface,artifact.kind))invalid(`manualQa.surfaceEvidence ${surface} artifact ${artifact.kind} is incompatible.`,"manualQa.surfaceEvidence");return{id:textField(row["id"],`${field}.id`),criterionRef:textField(row["criterionRef"],`${field}.criterionRef`),surface,invocation:textField(row["invocation"],`${field}.invocation`),verdict:passedVerdict(row["verdict"],`${field}.verdict`),artifactRefs:artifacts.map((artifact)=>artifact.id)}})}function parseAdversarialCases(value,byId){if(!Array.isArray(value)||value.length===0)invalid("manualQa.adversarialCases must not be empty.","manualQa.adversarialCases");return value.map((item,index)=>{const field=`manualQa.adversarialCases[${index}]`;const row=section(item,field);const artifacts=referencedArtifacts(row["artifactRefs"],`${field}.artifactRefs`,byId);const verdict=adversarialVerdict(row,field);return{id:textField(row["id"],`${field}.id`),criterionRef:textField(row["criterionRef"],`${field}.criterionRef`),scenario:textField(row["scenario"],`${field}.scenario`),expectedBehavior:textField(row["expectedBehavior"],`${field}.expectedBehavior`),verdict:verdict.verdict,...verdict.reason===undefined?{}:{reason:verdict.reason},artifactRefs:artifacts.map((artifact)=>artifact.id)}})}function validateLegacyQualityGate(gate){const cleaner=section(gate["aiSlopCleaner"],"aiSlopCleaner");const verification=section(gate["verification"],"verification");const review=section(gate["codeReview"],"codeReview");const coverage=section(gate["criteriaCoverage"],"criteriaCoverage");if(cleaner["status"]!=="passed")invalid("aiSlopCleaner.status must be passed.","aiSlopCleaner.status");if(verification["status"]!=="passed")invalid("verification.status must be passed.","verification.status");const totalCriteria=numberField(coverage["totalCriteria"],"criteriaCoverage.totalCriteria");const passCount=numberField(coverage["passCount"],"criteriaCoverage.passCount");if(passCount<totalCriteria)invalid("criteriaCoverage.passCount must cover totalCriteria.","criteriaCoverage.passCount");const evidence=textField(review["evidence"],"codeReview.evidence");const approved=UNCONDITIONAL_APPROVAL_PATTERN.test(evidence);const recommendation=review["recommendation"]==="APPROVE"||approved&&(review["recommendation"]===undefined||typeof review["recommendation"]==="string"&&review["recommendation"].trim()==="")?"APPROVE":invalid("codeReview.recommendation must be APPROVE or codeReview.evidence should include UNCONDITIONAL APPROVAL.","codeReview.recommendation");const architectStatus=review["architectStatus"]==="CLEAR"||approved&&(review["architectStatus"]===undefined||typeof review["architectStatus"]==="string"&&review["architectStatus"].trim()==="")?"CLEAR":invalid("codeReview.architectStatus must be CLEAR or codeReview.evidence should include UNCONDITIONAL APPROVAL.","codeReview.architectStatus");const verificationEvidence=textField(verification["evidence"],"verification.evidence");const commands=stringArray(verification["commands"],"verification.commands");const covered=stringArray(coverage["adversarialClassesCovered"],"criteriaCoverage.adversarialClassesCovered");const base={codeReview:{by:"judge",recommendation,codeQualityStatus:"CLEAR",reportPath:"legacy",evidence,blockers:[]},manualQa:{by:"operator",status:"passed",evidence:verificationEvidence,surfaceEvidence:[],adversarialCases:[],artifactRefs:[]},gateReview:{by:"skeptic",recommendation:"APPROVE",reportPath:"legacy",evidence,blockers:[]},iteration:{fullRerun:true,status:"passed",rerunCommands:commands,evidence:verificationEvidence},criteriaCoverage:{totalCriteria,passCount,originalIntent:"legacy",desiredOutcome:"legacy",userOutcomeReview:"legacy",adversarialClassesCovered:covered}};return Object.assign(base,{aiSlopCleaner:{status:"passed",evidence:textField(cleaner["evidence"],"aiSlopCleaner.evidence")},verification:{status:"passed",commands,evidence:verificationEvidence},codeReview:Object.assign(base.codeReview,{architectStatus})})}function validateQualityGate(input,opts){const gate=section(input,"qualityGate");if(opts===undefined&&gate["aiSlopCleaner"]!==undefined)return validateLegacyQualityGate(gate);const codeReview=section(gate["codeReview"],"codeReview");const manualQa=section(gate["manualQa"],"manualQa");const gateReview=section(gate["gateReview"],"gateReview");const iteration=section(gate["iteration"],"iteration");const coverage=section(gate["criteriaCoverage"],"criteriaCoverage");const totalCriteria=numberField(coverage["totalCriteria"],"criteriaCoverage.totalCriteria");const passCount=numberField(coverage["passCount"],"criteriaCoverage.passCount");const artifactRefs=parseArtifactRefs(manualQa["artifactRefs"],opts);const byId=new Map(artifactRefs.map((ref)=>[ref.id,ref]));const surfaceEvidence=parseSurfaceEvidence(manualQa["surfaceEvidence"],byId);const adversarialCases=parseAdversarialCases(manualQa["adversarialCases"],byId);const codeReportPath=textField(codeReview["reportPath"],"codeReview.reportPath");const gateReportPath=textField(gateReview["reportPath"],"gateReview.reportPath");checkFile(codeReportPath,"codeReview.reportPath",opts);checkFile(gateReportPath,"gateReview.reportPath",opts);validateCriteriaCoverage({totalCriteria,passCount,refs:[...surfaceEvidence,...adversarialCases].map((item)=>item.criterionRef)},opts);return{codeReview:{by:reviewerRoleField(codeReview["by"],REVIEWER_ROLES.codeReview,"codeReview.by"),recommendation:literal(codeReview["recommendation"],"APPROVE","codeReview.recommendation"),codeQualityStatus:codeQualityStatusField(codeReview["codeQualityStatus"],"codeReview.codeQualityStatus"),reportPath:codeReportPath,evidence:textField(codeReview["evidence"],"codeReview.evidence"),blockers:emptyBlockers(codeReview["blockers"],"codeReview.blockers")},manualQa:{by:reviewerRoleField(manualQa["by"],REVIEWER_ROLES.manualQa,"manualQa.by"),status:literal(manualQa["status"],"passed","manualQa.status"),evidence:textField(manualQa["evidence"],"manualQa.evidence"),surfaceEvidence,adversarialCases,artifactRefs},gateReview:{by:reviewerRoleField(gateReview["by"],REVIEWER_ROLES.gateReview,"gateReview.by"),recommendation:literal(gateReview["recommendation"],"APPROVE","gateReview.recommendation"),reportPath:gateReportPath,evidence:textField(gateReview["evidence"],"gateReview.evidence"),blockers:emptyBlockers(gateReview["blockers"],"gateReview.blockers")},iteration:{fullRerun:literal(iteration["fullRerun"],true,"iteration.fullRerun"),status:literal(iteration["status"],"passed","iteration.status"),rerunCommands:stringArray(iteration["rerunCommands"],"iteration.rerunCommands"),evidence:textField(iteration["evidence"],"iteration.evidence")},criteriaCoverage:{totalCriteria,passCount,originalIntent:textField(coverage["originalIntent"],"criteriaCoverage.originalIntent"),desiredOutcome:textField(coverage["desiredOutcome"],"criteriaCoverage.desiredOutcome"),userOutcomeReview:textField(coverage["userOutcomeReview"],"criteriaCoverage.userOutcomeReview"),adversarialClassesCovered:stringArray(coverage["adversarialClassesCovered"],"criteriaCoverage.adversarialClassesCovered")}}}function workLoopFail2(message,code){throw new WorkLoopError(message,code)}function normalizeObjective2(value){return value.replace(/\s+/g," ").trim()}function nonEmptyEvidence2(value){const trimmed=value.trim();return trimmed||workLoopFail2("Evidence must be a non-empty string.","WORK_LOOP_EVIDENCE_REQUIRED")}function findGoal2(plan,goalId){const goal=plan.goals.find((candidate)=>candidate.id===goalId);return goal??workLoopFail2(`Unknown work-loop id: ${goalId}.`,"WORK_LOOP_GOAL_NOT_FOUND")}function textMentionsWorkLoopPlanArtifact(value){const normalized=(value??"").toLowerCase();return normalized.includes(WORK_LOOP_DIR.toLowerCase())||normalized.includes(WORK_LOOP_GOALS.toLowerCase())||normalized.includes(WORK_LOOP_LEDGER.toLowerCase())}function textMentionsGoalId(value,goalId){return(value??"").toLowerCase().includes(goalId.toLowerCase())}function textHasCompletionValidationEvidence(value){const normalized=(value??"").toLowerCase();const done=/\b(?:planned work|implementation|deliverables?|scope|task|work)\b/.test(normalized)&&/\b(?:done|complete|completed|finished|shipped)\b/.test(normalized);const verified=/\b(?:validation|verification|tests?|build|lint|review|quality gate|code-review)\b/.test(normalized)&&/\b(?:passed|complete|completed|clean|green|approve|approved|clear)\b/.test(normalized);return done&&verified}async function snapshotObjectiveMapsToWorkLoopPlan(repoRoot,snapshotObjective,scope){const actual=normalizeObjective2(snapshotObjective).toLowerCase();if(textMentionsWorkLoopPlanArtifact(actual))return true;if(actual.length<24||!existsSync2(workLoopBriefPath(repoRoot,scope)))return false;try{const brief=normalizeObjective2(await readFile3(workLoopBriefPath(repoRoot,scope),"utf8")).toLowerCase();return brief.length>=24&&(brief.includes(actual)||actual.includes(brief))}catch(error){if(error instanceof Error)return false;throw error}}async function canReconcileCompletedTaskScopedAggregateSnapshot(repoRoot,plan,goal,snapshotObjective,evidence,scope){if(hostGoalMode(plan)!=="aggregate")return false;if(goal.status!=="in_progress"||plan.activeGoalId!==goal.id)return false;if(isFinalRunCompletionCandidate(plan,goal))return snapshotObjectiveMapsToWorkLoopPlan(repoRoot,snapshotObjective,scope);if(!textMentionsWorkLoopPlanArtifact(evidence)||!textMentionsGoalId(evidence,goal.id))return false;if(!textHasCompletionValidationEvidence(evidence))return false;return snapshotObjectiveMapsToWorkLoopPlan(repoRoot,snapshotObjective,scope)}async function canReconcileActiveFinalTaskScopedAggregateSnapshot(repoRoot,plan,goal,snapshotObjective,evidence,scope){if(hostGoalMode(plan)!=="aggregate")return false;if(goal.status!=="in_progress"||plan.activeGoalId!==goal.id)return false;if(!isFinalRunCompletionCandidate(plan,goal))return false;if(!textHasCompletionValidationEvidence(evidence))return false;return snapshotObjectiveMapsToWorkLoopPlan(repoRoot,snapshotObjective,scope)}function buildCompletedLegacyGoalRemediation(goal){return["If get_goal returns a different completed objective, do not repeat --status complete in this Auggie session.",`Record a non-terminal blocker with: ${INSTALLED_WORK_LOOP_COMMAND} checkpoint --goal-id ${goal.id} --status blocked --evidence "<completed host goal blocks native goal activation in this Auggie session>" --host-goal-json "<different completed get_goal JSON or path>".`,"Then continue only from a host goal context with no active/completed conflicting goal, in the same repo/worktree, and create the intended goal there."].join(" ")}function buildTaskScopedAggregateReconciliationHint(goal,final){if(final){return` Final task-scoped aggregate reconciliation requires the checkpoint goal to be the active in-progress final ASTERLINE goal and the completed get_goal objective to map to the work-loop brief or artifact. ${buildCompletedLegacyGoalRemediation(goal)}`}return` Completed task-scoped aggregate reconciliation requires the checkpoint goal to be the active in-progress ASTERLINE goal, evidence that names that active ASTERLINE goal id, names .asterline/work-loop/goals.json or ledger.jsonl, includes completed implementation plus validation/review evidence, and a get_goal objective that maps to the work-loop brief/artifact. ${buildCompletedLegacyGoalRemediation(goal)}`}async function readJsonInput(raw,repoRoot){if(raw===undefined||raw.trim()==="")return;const trimmed=raw.trim();try{return JSON.parse(trimmed)}catch(error){if(!(error instanceof SyntaxError))throw error}const path=resolve4(repoRoot,trimmed);if(!existsSync2(path))return workLoopFail2("Quality gate JSON is neither valid JSON nor a readable path.","WORK_LOOP_JSON_INPUT_INVALID");try{return JSON.parse(await readFile3(path,"utf8"))}catch(error){return workLoopFail2(`Quality gate path does not contain valid JSON${error instanceof Error?`: ${error.message}`:"."}`,"WORK_LOOP_JSON_INPUT_INVALID")}}function makeAggregateCompletion(now,evidence,hostGoal){return{status:"complete",completedAt:now,evidence,hostGoal}}function applyBlockedOrFailed(goal,plan,status,evidence,now){const signature=classifyExternalAuthorizationBlocker(evidence);const occurrences=signature===null?0:sameBlockerOccurrences(plan,signature)+1;const needsDecision=signature!==null&&occurrences>=3;goal.status=needsDecision?"needs_user_decision":status;goal.updatedAt=now;if(status==="failed"||needsDecision){goal.failedAt=now;goal.failureReason=evidence}if(status==="blocked"||needsDecision)goal.blockedReason=evidence;if(signature!==null){goal.blockerSignature=signature;goal.blockerOccurrenceCount=occurrences;goal.requiredExternalDecision=`Resolve external authorization: ${signature}`}if(needsDecision)goal.nonRetriable=true;if(plan.activeGoalId===goal.id)delete plan.activeGoalId}function ledgerKind2(status,goal,aggregateCompletion){if(aggregateCompletion!==undefined)return"aggregate_completed";if(status==="complete")return"goal_completed";if(goal.status==="needs_user_decision")return"goal_needs_user_decision";return status==="blocked"?"goal_blocked":"goal_failed"}function buildLedger(now,args,goal,qualityGate,hostGoal,aggregateCompletion){const entry={at:now,kind:ledgerKind2(args.status,goal,aggregateCompletion),goalId:goal.id,status:goal.status,evidence:args.evidence};if(hostGoal!==undefined)entry.hostGoal=hostGoal;if(qualityGate!==undefined)entry.qualityGate=qualityGate;if(goal.blockerSignature!==undefined)entry.blockerSignature=goal.blockerSignature;if(goal.blockerOccurrenceCount!==undefined)entry.blockerOccurrenceCount=goal.blockerOccurrenceCount;if(goal.requiredExternalDecision!==undefined)entry.requiredExternalDecision=goal.requiredExternalDecision;return entry}async function checkpointWorkLoop(repoRoot,args,scope){return withWorkLoopMutationLock(repoRoot,scope,async()=>{const plan=await readWorkLoopPlan(repoRoot,scope);const goal=findGoal2(plan,args.goalId);const evidence=nonEmptyEvidence2(args.evidence);const now=iso();let aggregateCompletion;let qualityGate;let hostGoal;if(args.status==="complete"){const aggregate=hostGoalMode(plan)==="aggregate";const final=isFinalRunCompletionCandidate(plan,goal);if(final){requireAllCriteriaPass(goal);requireAllPlanCriteriaPass(plan)}else if(aggregate)requireEssentialCriteriaPass(goal);else requireAllCriteriaPass(goal);const snapshot=await readHostGoalSnapshotInput(args.hostGoalJson,repoRoot);const reconciliation=reconcileHostGoalSnapshot(snapshot,{expectedObjective:expectedAsterlineObjective(plan,goal),...aggregate?{acceptedObjectives:compatibleAsterlineObjectives(plan)}:{},allowedStatuses:aggregate?final?["complete"]:["active"]:["complete"],requireSnapshot:true,requireComplete:!aggregate||final});hostGoal=reconciliation.snapshot.raw;if(!reconciliation.ok){const objective=snapshot?.objective;const mismatchedTaskObjective=snapshot?.available===true&&objective!==undefined&&normalizeObjective2(objective)!==normalizeObjective2(expectedAsterlineObjective(plan,goal));const completedTaskScoped=mismatchedTaskObjective&&snapshot.status==="complete"&&await canReconcileCompletedTaskScopedAggregateSnapshot(repoRoot,plan,goal,objective,evidence,scope);const activeFinalTaskScoped=mismatchedTaskObjective&&snapshot.status==="active"&&await canReconcileActiveFinalTaskScopedAggregateSnapshot(repoRoot,plan,goal,objective,evidence,scope);const taskScoped=completedTaskScoped||activeFinalTaskScoped;if(!taskScoped)throw new WorkLoopError(`${formatHostGoalReconciliation(reconciliation)}${aggregate&&snapshot?.status==="complete"&&objective!==undefined?buildTaskScopedAggregateReconciliationHint(goal,final):""}`,"WORK_LOOP_ASTERLINE_SNAPSHOT_MISMATCH");aggregateCompletion=makeAggregateCompletion(now,evidence,hostGoal)}if(final)aggregateCompletion=makeAggregateCompletion(now,evidence,hostGoal);if(final||aggregateCompletion!==undefined){const gateOptions=plan.evidenceLayoutVersion===2?{repoRoot,fs:{existsSync:existsSync2,lstatSync,realpathSync},currentAttemptDir:workLoopAttemptEvidenceDir(goal.id,goal.attempt,scope),criteria:plan.goals.flatMap((item)=>item.successCriteria.map((criterion)=>({goalId:item.id,criterionId:criterion.id})))}:undefined;qualityGate=validateQualityGate(await readJsonInput(args.qualityGateJson,repoRoot),gateOptions)}goal.status="complete";goal.completedAt=now;goal.evidence=evidence;delete goal.failedAt;delete goal.failureReason;clearGoalBlockerFields(goal);if(plan.activeGoalId===goal.id)delete plan.activeGoalId}else applyBlockedOrFailed(goal,plan,args.status,evidence,now);goal.updatedAt=now;if(aggregateCompletion!==undefined)plan.aggregateCompletion=aggregateCompletion;plan.updatedAt=now;await writePlan(repoRoot,plan,scope);const ledgerEntry=buildLedger(now,args,goal,qualityGate,hostGoal,aggregateCompletion);await appendLedger(repoRoot,ledgerEntry,scope);return aggregateCompletion===undefined?{plan,goal,ledgerEntry}:{plan,goal,ledgerEntry,aggregateCompletion}})}import{readFile as readFile4}from"node:fs/promises";var VALUE_FLAGS=new Set("--brief --brief-file --session-id --host-goal-mode --goal --goal-id --criterion-id --status --evidence --notes --host-goal-json --quality-gate-json --kind --rationale --title --objective --target-goal-id --source --after-json --directive-json --directive-file --idempotency-key".split(" "));var SUBCOMMANDS=new Set("create-goals status complete-goals criteria record-evidence checkpoint steer add-goal record-review-blockers".split(" "));function hasFlag(argv,flag){return argv.includes(flag)}function readValue(argv,flag){const index=argv.indexOf(flag);if(index>=0){const next=argv[index+1];return next===undefined||next.startsWith("--")?undefined:next}const prefix=`${flag}=`;return argv.find((arg)=>arg.startsWith(prefix))?.slice(prefix.length)}function parseGoalArg(argv){return readValue(argv,"--goal-id")??readValue(argv,"--goal")}async function readStdin(){const chunks=[];for await(const chunk of process.stdin)chunks.push(Buffer.isBuffer(chunk)?chunk:Buffer.from(chunk));return Buffer.concat(chunks).toString("utf8")}function positionalText(argv){const words=[];for(let index=SUBCOMMANDS.has(argv[0]??"")?1:0;index<argv.length;index+=1){const arg=argv[index];if(arg===undefined)continue;if(VALUE_FLAGS.has(arg)){index+=1;continue}if(arg.startsWith("--"))continue;words.push(arg)}return words.join(" ").trim()}function looksLikeJson(value){const trimmed=value.trim();return trimmed.startsWith("{")||trimmed.startsWith("[")}async function readJsonInput2(value){if(value===undefined)return;try{return JSON.parse(looksLikeJson(value)?value:await readFile4(value,"utf8"))}catch(error){const message=error instanceof Error?error.message:"unknown error";throw new WorkLoopError(`Invalid JSON input: ${message}`,"WORK_LOOP_JSON_INPUT_INVALID",{cause:error})}}async function parseHostGoalJson(value){if(value===undefined)return;const raw=looksLikeJson(value)?value:await readFile4(value,"utf8");try{JSON.parse(raw);return raw}catch(error){const message=error instanceof Error?error.message:"unknown error";throw new WorkLoopError(`Invalid --host-goal-json: ${message}`,"WORK_LOOP_ASTERLINE_GOAL_JSON_INVALID",{cause:error})}}function required(argv,flag,code){const value=readValue(argv,flag)?.trim();if(value)return value;throw new WorkLoopError(`Missing ${flag}.`,code,{details:{flag}})}function evidenceStatus(value){switch(value){case"pass":return"pass";case"fail":return"fail";case"blocked":return"blocked";default:throw new WorkLoopError("Invalid --status; expected pass, fail, or blocked.","WORK_LOOP_EVIDENCE_STATUS_INVALID",{details:{status:value}})}}function parseRecordEvidenceArgs(argv){const result={goalId:required(argv,"--goal-id","WORK_LOOP_GOAL_ID_REQUIRED"),criterionId:required(argv,"--criterion-id","WORK_LOOP_CRITERION_ID_REQUIRED"),status:evidenceStatus(required(argv,"--status","WORK_LOOP_EVIDENCE_STATUS_REQUIRED")),evidence:required(argv,"--evidence","WORK_LOOP_EVIDENCE_REQUIRED")};const notes=readValue(argv,"--notes")?.trim();return notes?{...result,notes}:result}var WORK_LOOP_HELP=`Usage:
  ${INSTALLED_WORK_LOOP_COMMAND} create-goals --brief "..." [--brief-file <path>] [--from-stdin] [--host-goal-mode aggregate|per_story] [--force] [--json]
  ${INSTALLED_WORK_LOOP_COMMAND} status [--json]
  ${INSTALLED_WORK_LOOP_COMMAND} complete-goals [--retry-failed] [--json]
  ${INSTALLED_WORK_LOOP_COMMAND} criteria --goal-id <id> [--json]
  ${INSTALLED_WORK_LOOP_COMMAND} record-evidence --goal-id <id> --criterion-id <id> --status pass|fail|blocked --evidence "..." [--notes "..."] [--json]
  ${INSTALLED_WORK_LOOP_COMMAND} checkpoint --goal-id <id> --status complete|failed|blocked --evidence "..." --host-goal-json <...> [--quality-gate-json <...>] [--json]
  ${INSTALLED_WORK_LOOP_COMMAND} steer --kind <kind> ... --evidence "..." --rationale "..." [--json]
  ${INSTALLED_WORK_LOOP_COMMAND} add-goal --title "..." --objective "..." [--json]
  ${INSTALLED_WORK_LOOP_COMMAND} record-review-blockers --goal-id <id> --title "..." --objective "..." --evidence "..." --host-goal-json <...> [--json]

All subcommands accept [--session-id <id>] to isolate state under .asterline/work-loop/<id>/; without it, Asterline session env is used when present.`;function printJson(value){process.stdout.write(`${JSON.stringify(value,null,2)}
`)}function printJsonError(error){if(error instanceof WorkLoopError){printJson({ok:false,error:{code:error.code,message:error.message,...error.details===undefined?{}:{details:error.details}}});return}if(error instanceof Error){printJson({ok:false,error:{code:"WORK_LOOP_UNEXPECTED",message:error.message}});return}printJson({ok:false,error:{code:"WORK_LOOP_UNKNOWN",message:"unknown error"}})}function criteriaCounts(goal){let pass=0;for(const criterion of goal.successCriteria)if(criterion.status==="pass")pass+=1;return{pass,total:goal.successCriteria.length}}function printStatus(plan){let totalCriteria=0;let passCriteria=0;const lines=["work-loop status","","goals:"];for(const goal of plan.goals){const counts=criteriaCounts(goal);totalCriteria+=counts.total;passCriteria+=counts.pass;const marker=goal.id===plan.activeGoalId?"*":"-";lines.push(`${marker} ${goal.id} [${goal.status}] ${goal.title} (criteria: ${counts.pass}/${counts.total})`)}lines.push("","summary:",`total goals: ${plan.goals.length}`,`criteria: ${passCriteria}/${totalCriteria} pass`);process.stdout.write(`${lines.join(`
`)}
`)}function blockedDecisionHandoff(plan){const blocked=plan.goals.find((goal)=>goal.status==="needs_user_decision"&&goal.nonRetriable);if(blocked===undefined)return"";return["work-loop: blocked on repeated external authorization; no retryable failed goals remain.",`Goal: ${blocked.id} - ${blocked.title}`,`Required external decision: ${blocked.requiredExternalDecision??"provide the missing authorization or choose a different unblock path"}.`,"Do not run complete-goals --retry-failed again until external state changes or the user authorizes an unblock path."].join(`
`)}function normalizeHostGoalMode(value){if(value===undefined)return"aggregate";if(value==="aggregate"||value==="per_story")return value;throw new WorkLoopError("Invalid --host-goal-mode; expected aggregate or per_story.","WORK_LOOP_ASTERLINE_GOAL_MODE_INVALID",{details:{value}})}var SOURCES=["user_prompt_submit","finding","cli"];function isKind(value){return value!==undefined&&WORK_LOOP_STEERING_MUTATION_KINDS.some((kind)=>kind===value)}function isSource(value){return value!==undefined&&SOURCES.some((source)=>source===value)}function isModel(value){return WORK_LOOP_SUCCESS_CRITERION_USER_MODELS.some((model)=>model===value)}function fail(message,code,details){throw new WorkLoopError(message,code,{details})}function text(value,field){if(value===undefined)return;const trimmed=value.trim();if(trimmed.length>0)return trimmed;return fail(`Empty ${field}.`,"WORK_LOOP_STEERING_FIELD_EMPTY",{field})}function required2(argv,flag){const value=text(readValue(argv,flag),flag);return value??fail(`Missing ${flag}.`,"WORK_LOOP_STEERING_FIELD_REQUIRED",{flag})}function requiredGoal(argv){const value=text(parseGoalArg(argv),"--goal-id");return value??fail("Missing --goal-id.","WORK_LOOP_GOAL_ID_REQUIRED",{flag:"--goal-id"})}function readObject(value,key){return Object.entries(value).find(([name])=>name===key)?.[1]}function isPlain(value){return typeof value==="object"&&value!==null&&!Array.isArray(value)}function objectText(value,key){const candidate=readObject(value,key);return typeof candidate==="string"?candidate:undefined}function parseSteeringKind(argv){const value=readValue(argv,"--kind");if(isKind(value))return value;return value===undefined?fail("Missing --kind.","WORK_LOOP_STEERING_KIND_REQUIRED",{flag:"--kind"}):fail(`Invalid --kind: ${value}.`,"WORK_LOOP_STEERING_KIND_INVALID",{value,expected:WORK_LOOP_STEERING_MUTATION_KINDS})}function parseSteeringSource(argv){const value=readValue(argv,"--source");if(value===undefined)return"cli";return isSource(value)?value:fail(`Invalid --source: ${value}.`,"WORK_LOOP_STEERING_SOURCE_INVALID",{value,expected:SOURCES})}function child(value){if(!isPlain(value))return null;const title=text(objectText(value,"title"),"title");const objective=text(objectText(value,"objective"),"objective");if(title===undefined||objective===undefined)return null;return{title,objective}}async function children(argv,flag,needed){const input=needed?required2(argv,flag):text(readValue(argv,flag),flag);if(input===undefined)return[];const raw=await readJsonInput2(input);if(!Array.isArray(raw))return fail(`${flag} must be a JSON array.`,"WORK_LOOP_STEERING_JSON_ARRAY_REQUIRED",{flag});const parsed=[];for(const item of raw){const next=child(item);if(next===null)return fail(`${flag} entries require title/objective.`,"WORK_LOOP_STEERING_CHILD_INVALID",{flag});parsed.push(next)}return parsed}async function stringArray2(argv,flag){const raw=await readJsonInput2(required2(argv,flag));if(!Array.isArray(raw))return fail(`${flag} must be a JSON array.`,"WORK_LOOP_STEERING_JSON_ARRAY_REQUIRED",{flag});const values=[];for(const item of raw){if(typeof item!=="string")return fail(`${flag} entries must be strings.`,"WORK_LOOP_STEERING_STRING_ARRAY_REQUIRED",{flag});values.push(text(item,flag)??"")}return values}function model(value){const trimmed=text(value,"--user-model");if(trimmed===undefined)return;return isModel(trimmed)?trimmed:fail(`Invalid --user-model: ${trimmed}.`,"WORK_LOOP_STEERING_USER_MODEL_INVALID",{value:trimmed,expected:WORK_LOOP_SUCCESS_CRITERION_USER_MODELS})}function neverKind(kind){return fail(`Unsupported steering kind: ${String(kind)}.`,"WORK_LOOP_STEERING_KIND_UNSUPPORTED",{kind})}async function parseSteeringProposal(argv){const kind=parseSteeringKind(argv);const source=parseSteeringSource(argv);const idempotencyKey=text(readValue(argv,"--idempotency-key"),"--idempotency-key");const base={kind,source,evidence:required2(argv,"--evidence"),rationale:required2(argv,"--rationale"),...idempotencyKey===undefined?{}:{idempotencyKey}};switch(kind){case"add_subgoal":return normalizeSteeringProposal({...base,title:required2(argv,"--title"),objective:required2(argv,"--objective")});case"split_subgoal":{const goalId=requiredGoal(argv);return normalizeSteeringProposal({...base,goalId,targetGoalId:goalId,childGoals:await children(argv,"--children",true)})}case"reorder_pending":return normalizeSteeringProposal({...base,pendingOrder:await stringArray2(argv,"--order")});case"revise_pending_wording":{const goalId=requiredGoal(argv);const revisedTitle=readValue(argv,"--title");const revisedObjective=readValue(argv,"--objective");if(revisedTitle===undefined&&revisedObjective===undefined)return fail("revise_pending_wording requires --title or --objective.","WORK_LOOP_STEERING_UPDATE_REQUIRED",{kind});return normalizeSteeringProposal({...base,goalId,targetGoalId:goalId,...revisedTitle===undefined?{}:{revisedTitle},...revisedObjective===undefined?{}:{revisedObjective}})}case"revise_criterion":{const goalId=requiredGoal(argv);const criterionId=required2(argv,"--criterion-id");const scenario=readValue(argv,"--scenario");const expectedEvidence=readValue(argv,"--expected-evidence");const userModel=model(readValue(argv,"--user-model"));if(scenario===undefined&&expectedEvidence===undefined&&userModel===undefined)return fail("revise_criterion requires scenario, expected-evidence, or user-model.","WORK_LOOP_STEERING_UPDATE_REQUIRED",{kind});return normalizeSteeringProposal({...base,goalId,targetGoalId:goalId,criterionId,...scenario===undefined?{}:{scenario},...expectedEvidence===undefined?{}:{expectedEvidence},...userModel===undefined?{}:{userModel}})}case"annotate_ledger":return normalizeSteeringProposal(base);case"mark_blocked_superseded":{const goalId=requiredGoal(argv);const childGoals=await children(argv,"--replacements",false);return normalizeSteeringProposal({...base,goalId,targetGoalId:goalId,...childGoals.length===0?{}:{childGoals}})}default:return neverKind(kind)}}function normalizedChildren(values){if(values===undefined)return;return values.map((item)=>({title:text(item.title,"child.title")??"",objective:text(item.objective,"child.objective")??""}))}function normalizedStrings(values,field){if(values===undefined)return;return values.map((value)=>text(value,field)??"")}function normalizeSteeringProposal(proposal){const evidence=text(proposal.evidence,"evidence")??"";const rationale=text(proposal.rationale,"rationale")??"";const goalId=text(proposal.goalId,"goalId");const targetGoalId=text(proposal.targetGoalId,"targetGoalId");const targetGoalIds=normalizedStrings(proposal.targetGoalIds,"targetGoalIds");const criterionId=text(proposal.criterionId,"criterionId");const title=text(proposal.title,"title");const objective=text(proposal.objective,"objective");const revisedTitle=text(proposal.revisedTitle,"revisedTitle");const revisedObjective=text(proposal.revisedObjective,"revisedObjective");const blockedReason=text(proposal.blockedReason,"blockedReason");const directiveText=text(proposal.directiveText,"directiveText");const promptSignature=text(proposal.promptSignature,"promptSignature");const idempotencyKey=text(proposal.idempotencyKey,"idempotencyKey");const scenario=text(proposal.scenario,"scenario");const expectedEvidence=text(proposal.expectedEvidence,"expectedEvidence");const childGoals=normalizedChildren(proposal.childGoals);const pendingOrder=normalizedStrings(proposal.pendingOrder,"pendingOrder");return{kind:proposal.kind,source:proposal.source,evidence,rationale,...goalId===undefined?{}:{goalId},...targetGoalId===undefined?{}:{targetGoalId},...targetGoalIds===undefined?{}:{targetGoalIds},...criterionId===undefined?{}:{criterionId},...title===undefined?{}:{title},...objective===undefined?{}:{objective},...childGoals===undefined?{}:{childGoals},...revisedTitle===undefined?{}:{revisedTitle},...revisedObjective===undefined?{}:{revisedObjective},...pendingOrder===undefined?{}:{pendingOrder},...blockedReason===undefined?{}:{blockedReason},...proposal.after===undefined?{}:{after:proposal.after},...directiveText===undefined?{}:{directiveText},...promptSignature===undefined?{}:{promptSignature},...idempotencyKey===undefined?{}:{idempotencyKey},...proposal.now===undefined?{}:{now:proposal.now},...scenario===undefined?{}:{scenario},...expectedEvidence===undefined?{}:{expectedEvidence},...proposal.userModel===undefined?{}:{userModel:proposal.userModel}}}function printSteerResult(result,json){if(json){printJson({ok:result.accepted,accepted:result.accepted,rejectedReasons:result.rejectedReasons,deduped:result.deduped,audit:result.audit,plan:result.plan});return}const outcome=result.deduped?"deduped":result.accepted?"accepted":"rejected";process.stdout.write(`work-loop steer: ${outcome} ${result.audit.kind}
`);if(result.rejectedReasons.length>0)process.stdout.write(`rejected: ${result.rejectedReasons.join("; ")}
`);if(result.audit.idempotencyKey!==undefined)process.stdout.write(`idempotency-key: ${result.audit.idempotencyKey}
`);printStatus(result.plan)}function buildHostGoalInstruction(args){const mode=hostGoalMode(args.plan);const createGoal=buildCreateGoalPayload(args.plan,args.goal);const isFinal=args.isFinal??isFinalRunCompletionCandidate(args.plan,args.goal);return{text:buildText(mode,args.plan,args.goal,createGoal,isFinal),json:createGoal}}function buildCreateGoalPayload(plan,goal){return{objective:expectedAsterlineObjective(plan,goal)}}function buildText(mode,plan,goal,createGoal,isFinal){return joinLines([mode==="aggregate"?"WorkLoop aggregate-goal handoff":"WorkLoop active-goal handoff",`Mode: ${mode}`,`Plan: ${plan.goalsPath}`,`Ledger: ${plan.ledgerPath}`,`Goal: ${goal.id} — ${goal.title}`,"",...activeGoalLines(goal),"",...successCriteriaLines(goal.successCriteria),"","host goal integration constraints:","- Use the native goal activation payload exactly as rendered: objective only.","- Goals are unlimited. Do not add numeric limits.",...modeConstraintLines(mode,isFinal),finalSection(plan,goal,isFinal,mode==="aggregate"),...checkpointLines(plan,mode),"","native goal activation payload:",JSON.stringify(createGoal,null,2)])}function modeConstraintLines(mode,isFinal){if(mode==="per_story"){return["- First call get_goal. If no active goal exists, call native goal activation with the payload below.","- If a different active host goal exists, finish/checkpoint that goal before starting this work-loop.","- Work only this goal until its completion audit passes."]}return["- host goal = the whole Asterline work-loop run; ASTERLINE G001/G002/etc. = ledger stories.","- First call get_goal. If no active goal exists, call native goal activation with the aggregate payload below.","- If get_goal reports the same aggregate objective as active, continue this ASTERLINE story without creating a new host goal.","- If a different active or incomplete host goal exists, finish/checkpoint that goal before starting this work-loop.",isFinal?"- This is the final story; update_goal is allowed only after the mandatory quality gate passes.":"- This is not the final story: do not call update_goal yet; the aggregate host goal must remain active while later ASTERLINE stories remain."]}function checkpointLines(plan,mode){const failureLine=`- If blocked or failed, checkpoint with --status failed and the failure evidence; rerun complete-goals${sessionOption(plan)} --retry-failed to resume.`;if(mode==="per_story")return[failureLine];return["- Checkpoint this ASTERLINE story with a fresh get_goal snapshot whose objective matches the aggregate payload.",failureLine]}function activeGoalLines(goal){return["Active goal:",`- id: ${goal.id}`,`- title: ${goal.title}`,`- objective: ${goal.objective}`]}function successCriteriaLines(criteria){if(criteria.length===0)return["Success criteria:","- No success criteria recorded for this goal."];return["Success criteria:",...criteria.map(formatCriterionLine)]}function formatCriterionLine(criterion){const remainingWork=criterion.status==="pending"?" remaining work:":"";return`-${remainingWork} [${criterion.id}] (${criterion.userModel}) ${criterion.scenario} — expect: ${criterion.expectedEvidence} — status: ${criterion.status}`}function finalSection(plan,goal,isFinal,aggregate){if(!isFinal)return"- This is not the final work-loop story; do not run the final clean-ai-code/review-pass gate yet.";const option=sessionOption(plan);const blockerCommand=`${INSTALLED_WORK_LOOP_COMMAND} record-review-blockers${option} --goal-id ${goal.id} --title "Resolve final code-review blockers" --objective "<blocker-resolution objective>" --evidence "<review findings>" --host-goal-json "<active get_goal JSON or path>"`;const checkpointCommand=`${INSTALLED_WORK_LOOP_COMMAND} checkpoint${option} --goal-id ${goal.id} --status complete --evidence "<tests/files/PR evidence>" --host-goal-json "<fresh complete get_goal JSON or path>" --quality-gate-json "<quality gate JSON or path>"`;return joinLines(["Final story — run mandatory quality gate before update_goal:","- Run clean-ai-code on changed files even when it is a no-op, rerun verification, then run review-pass. Auggie may split bounded review tasks in parallel but does not provide persistent team messaging, resumption, or threads.","- Derive coverage from the current plan: reference every plan criterion exactly once across QA rows, using goal-qualified criterionRef values when criterion ids repeat.","- If the final review is not APPROVE with architect status CLEAR, do not call update_goal. Record blocker work first:",`  ${blockerCommand}`,aggregate?'- If the final review is clean, call update_goal({status: "complete"}), call get_goal again, then checkpoint the aggregate story:':'- If the final review is clean, call update_goal({status: "complete"}), call get_goal again, then checkpoint:',`  ${checkpointCommand}`])}function sessionOption(plan){const prefix=".asterline/work-loop/";const suffix="/goals.json";if(!plan.goalsPath.startsWith(prefix)||!plan.goalsPath.endsWith(suffix))return"";const sessionId=plan.goalsPath.slice(prefix.length,-suffix.length);return sessionId.length===0?"":` --session-id ${sessionId}`}function joinLines(lines){return lines.join(`
`)}import{existsSync as existsSync3}from"node:fs";import{writeFile as writeFile2}from"node:fs/promises";function cleanLine(line){return line.replace(/^\s*(?:[-*+]\s+|\d+[.)]\s+)/,"").trim()}function normalizeObjective3(value){return value.replace(/\s+/g," ").trim()}function titleFromObjective(objective,fallback){const firstLine=objective.split(/\r?\n/).map((line)=>line.trim()).find(Boolean)??fallback;return firstLine.length>72?`${firstLine.slice(0,69).trimEnd()}...`:firstLine}function normalizeGoalId(title,index){const slug=title.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"").slice(0,36).replace(/-+$/g,"");return`G${String(index+1).padStart(3,"0")}${slug?`-${slug}`:""}`}function assertNonEmpty(value,label){const trimmed=value?.trim();if(!trimmed)throw new WorkLoopError(`Missing ${label}.`,"WORK_LOOP_ARGUMENT_MISSING");return trimmed}function truncateObjective(objective){return objective.length>80?`${objective.slice(0,77).trimEnd()}...`:objective}function seedDefaultSuccessCriteria(goalIndex,objective){const subject=truncateObjective(normalizeObjective3(objective)||`Goal ${goalIndex+1}`);const rows=[["C001","happy",`happy path for: ${subject}`,`Replace via revise_criterion with observable happy-path proof for goal ${goalIndex+1}.`,true],["C002","edge","edge case (boundary/empty/malformed)",`Replace via revise_criterion with boundary or malformed-input proof for: ${subject}.`,true],["C003","regression","regression: adjacent surface still works",`Replace via revise_criterion with regression proof for neighboring behavior after: ${subject}.`,false]];return rows.map(([id,userModel,scenario,expectedEvidence,essential])=>({id,scenario,userModel,expectedEvidence,essential,capturedEvidence:null,status:"pending"}))}function deriveGoalCandidates(brief){const bulletGoals=brief.split(/\r?\n/).map((line)=>({original:line,cleaned:normalizeObjective3(cleanLine(line))})).filter(({cleaned})=>cleaned.length>0&&cleaned.length<=1200).filter(({original,cleaned},index,all)=>/^\s*(?:[-*+]\s+|\d+[.)]\s+)/.test(original)&&all.findIndex((candidate)=>candidate.cleaned===cleaned)===index).map(({cleaned})=>cleaned);const paragraphs=brief.split(/\n\s*\n/).map(normalizeObjective3).filter((paragraph)=>paragraph.length>0&&!paragraph.startsWith("#"));const selected=(bulletGoals.length>0?bulletGoals:paragraphs).length>0?bulletGoals.length>0?bulletGoals:paragraphs:["Complete the requested project objective."];return selected.map((objective,index)=>({title:titleFromObjective(objective,`Goal ${index+1}`),objective}))}function makeGoal(title,objective,index,now){const cleanTitle=assertNonEmpty(title,"title");const cleanObjective=assertNonEmpty(objective,"objective");return{id:normalizeGoalId(cleanTitle,index),title:cleanTitle,objective:cleanObjective,status:"pending",successCriteria:seedDefaultSuccessCriteria(index,cleanObjective),attempt:0,createdAt:now,updatedAt:now}}function appendGoalToPlan(plan,title,objective,now){const goal=makeGoal(title,objective,plan.goals.length,now);plan.goals.push(goal);plan.updatedAt=now;return goal}function isScheduleEligible(goal){return goal.steeringStatus!=="superseded"&&goal.steeringStatus!=="blocked"}function clearGoalBlockerFields2(goal){for(const key of["blockedReason","blockerSignature","blockerOccurrenceCount","requiredExternalDecision","nonRetriable","failedAt","failureReason"])delete goal[key]}async function createWorkLoopPlan(repoRoot,args,scope){return withWorkLoopMutationLock(repoRoot,scope,async()=>{if(!args.force&&existsSync3(workLoopGoalsPath(repoRoot,scope))){const existing=await readWorkLoopPlan(repoRoot,scope);if(isWorkLoopDone(existing))throw completedPlanExistsError(scope);throw new WorkLoopError(`Refusing to overwrite existing ${workLoopGoalsRelativePath(scope)}; pass --force to recreate it.`,"WORK_LOOP_PLAN_EXISTS")}const now=iso();const goals=deriveGoalCandidates(args.brief).map((goal,index)=>makeGoal(goal.title,goal.objective,index,now));const plan={version:1,evidenceLayoutVersion:2,createdAt:now,updatedAt:now,briefPath:workLoopBriefRelativePath(scope),goalsPath:workLoopGoalsRelativePath(scope),ledgerPath:workLoopLedgerRelativePath(scope),hostGoalMode:args.hostGoalMode??"aggregate",goals};if(plan.hostGoalMode==="aggregate")plan.asterlineObjective=aggregateAsterlineObjectiveForScope(scope);await ensureWorkLoopDir(repoRoot,scope);await writeFile2(workLoopBriefPath(repoRoot,scope),args.brief.endsWith(`
`)?args.brief:`${args.brief}
`,"utf8");await writePlan(repoRoot,plan,scope);await writeFile2(workLoopLedgerPath(repoRoot,scope),"","utf8");await appendLedger(repoRoot,{at:now,kind:"plan_created",message:`${goals.length} goal(s) created`},scope);return plan})}function completedPlanExistsError(scope){return new WorkLoopError([`Existing work-loop aggregate is already complete at ${workLoopGoalsRelativePath(scope)}.`,`Start a new run with \`${INSTALLED_WORK_LOOP_COMMAND} create-goals --session-id <new-id> ...\` to isolate fresh state.`,"Use --force only when you intentionally want to overwrite the completed evidence."].join(" "),"WORK_LOOP_PLAN_EXISTS_COMPLETE")}async function addWorkLoopGoal(repoRoot,args,scope){return withWorkLoopMutationLock(repoRoot,scope,async()=>{const plan=await readWorkLoopPlan(repoRoot,scope);const now=iso();const goal=appendGoalToPlan(plan,args.title,args.objective,now);await writePlan(repoRoot,plan,scope);await appendLedger(repoRoot,{at:now,kind:"goal_added",goalId:goal.id,status:goal.status,message:goal.title},scope);return{plan,goal}})}async function startNextWorkLoop(repoRoot,args={},scope){return withWorkLoopMutationLock(repoRoot,scope,async()=>{const plan=await readWorkLoopPlan(repoRoot,scope);const now=iso();if(plan.aggregateCompletion?.status==="complete")return{done:true,plan};const existing=plan.goals.find((goal)=>goal.status==="in_progress"&&isScheduleEligible(goal));if(existing)return{plan,goal:existing,resumed:true};let next=plan.goals.find((goal)=>goal.status==="pending"&&isScheduleEligible(goal));if(!next&&args.retryFailed){next=plan.goals.find((goal)=>goal.status==="failed"&&!goal.nonRetriable&&isScheduleEligible(goal));if(next)await appendLedger(repoRoot,{at:now,kind:"goal_retried",goalId:next.id,status:"pending",...next.failureReason?{message:next.failureReason}:{}},scope)}if(!next)return{done:true,plan};next.status="in_progress";next.attempt+=1;next.startedAt=now;clearGoalBlockerFields2(next);next.updatedAt=now;plan.activeGoalId=next.id;plan.updatedAt=now;await writePlan(repoRoot,plan,scope);await appendLedger(repoRoot,{at:now,kind:"goal_started",goalId:next.id,status:next.status,message:`Attempt ${next.attempt}`},scope);return{plan,goal:next,resumed:false}})}function summarizeWorkLoopPlan(plan){const countStatus=(status)=>plan.goals.filter((goal)=>goal.status===status).length;const countCriteria=(status)=>plan.goals.reduce((sum,goal)=>sum+goal.successCriteria.filter((criterion)=>criterion.status===status).length,0);return{total:plan.goals.length,pending:countStatus("pending"),in_progress:countStatus("in_progress"),complete:countStatus("complete"),failed:countStatus("failed"),blocked:countStatus("blocked"),review_blocked:countStatus("review_blocked"),needs_user_decision:countStatus("needs_user_decision"),superseded:plan.goals.filter((goal)=>goal.steeringStatus==="superseded").length,criteria:{total:plan.goals.reduce((sum,goal)=>sum+goal.successCriteria.length,0),pass:countCriteria("pass"),pending:countCriteria("pending"),fail:countCriteria("fail"),blocked:countCriteria("blocked")}}}var BLOCKER_FIELDS="blockedReason blockerSignature blockerOccurrenceCount requiredExternalDecision nonRetriable failedAt failureReason completedAt blocker blockerEvidence blockerOccurrences blockedAt".split(" ");function workLoopError(message,code){throw new WorkLoopError(message,code)}function nextGoalId(plan){const max=plan.goals.reduce((current,goal)=>{const digits=/^G(\d+)/u.exec(goal.id)?.[1];return digits===undefined?current:Math.max(current,Number(digits))},0);return`G${String(max+1).padStart(3,"0")}`}function appendBlockerGoal(plan,args,now){const index=plan.goals.length;const goal={id:nextGoalId(plan),title:args.title,objective:args.objective,status:"pending",successCriteria:seedDefaultSuccessCriteria(index,args.objective),attempt:0,createdAt:now,updatedAt:now};plan.goals.push(goal);return goal}async function recordFinalReviewBlockers(repoRoot,args,scope){return withWorkLoopMutationLock(repoRoot,scope,async()=>{const plan=await readWorkLoopPlan(repoRoot,scope);const goal=plan.goals.find((candidate)=>candidate.id===args.goalId);if(goal===undefined)workLoopError(`Unknown work-loop id: ${args.goalId}`,"WORK_LOOP_GOAL_NOT_FOUND");if(goal.status!=="in_progress")workLoopError(`${goal.id} is ${goal.status}.`,"WORK_LOOP_GOAL_NOT_IN_PROGRESS");if(!isFinalRunCompletionCandidate(plan,goal))workLoopError(`${goal.id} is not final.`,"WORK_LOOP_NOT_FINAL_GOAL");const snapshot=await readHostGoalSnapshotInput(args.hostGoalJson,repoRoot);const aggregate=hostGoalMode(plan)==="aggregate";const reconciliation=reconcileHostGoalSnapshot(snapshot,{expectedObjective:expectedAsterlineObjective(plan,goal),...aggregate?{acceptedObjectives:compatibleAsterlineObjectives(plan)}:{},allowedStatuses:["active"],requireSnapshot:true,requireComplete:false});if(!reconciliation.ok)workLoopError(reconciliation.errors.join(" "),"WORK_LOOP_ASTERLINE_SNAPSHOT_MISMATCH");const now=iso();for(const field of BLOCKER_FIELDS)Reflect.deleteProperty(goal,field);goal.status="review_blocked";goal.reviewBlockedAt=now;goal.evidence=args.evidence;goal.updatedAt=now;if(plan.activeGoalId===goal.id)delete plan.activeGoalId;const newGoal=appendBlockerGoal(plan,args,now);plan.updatedAt=now;const hostGoal=reconciliation.snapshot.raw;const blockedEntry={at:now,kind:"goal_review_blocked",goalId:goal.id,status:goal.status,evidence:args.evidence,hostGoal};const addedEntry={at:now,kind:"goal_added",goalId:newGoal.id,status:newGoal.status,evidence:args.evidence,message:newGoal.title};const summaryEntry={at:now,kind:"goal_review_blocked",goalId:goal.id,status:goal.status,evidence:args.evidence,hostGoal,message:`Review blockers recorded; appended ${newGoal.id}.`};Reflect.set(summaryEntry,"kind","blocker_recorded");const ledgerEntries=[blockedEntry,addedEntry,summaryEntry];await writePlan(repoRoot,plan,scope);for(const entry of ledgerEntries)await appendLedger(repoRoot,entry,scope);return{plan,blockedGoal:goal,newGoal,ledgerEntries}})}function buildSteeringPlanSnapshot(plan,changedGoalIds){const snapshot={updatedAt:plan.updatedAt,goalCount:plan.goals.length,goalIds:plan.goals.map((goal)=>goal.id),goals:plan.goals.filter((goal)=>changedGoalIds.has(goal.id))};return plan.activeGoalId===undefined?snapshot:{...snapshot,activeGoalId:plan.activeGoalId}}function changedGoalIdsBetween(before,after){const beforeById=new Map(before.goals.map((goal)=>[goal.id,goal]));const changed=new Set;for(const goal of after.goals){const prior=beforeById.get(goal.id);if(prior===undefined||JSON.stringify(prior)!==JSON.stringify(goal))changed.add(goal.id);beforeById.delete(goal.id)}for(const id of beforeById.keys())changed.add(id);return changed}var SOURCES2=["user_prompt_submit","finding","cli"];var PROTECTED=new Set(["aggregateCompletion","asterlineObjective","asterlineObjectiveAliases","originalConstraints","qualityGate","status","completedAt","completionStatus"]);var isObject=(value)=>typeof value==="object"&&value!==null;var isPlain2=(value)=>isObject(value)&&!Array.isArray(value);var read=(value,key)=>Object.entries(value).find(([name])=>name===key)?.[1];var isText=(value)=>typeof value==="string"&&value.trim().length>0;var text2=(value,key)=>{const candidate=read(value,key);return isText(candidate)?candidate.trim():undefined};var isKind2=(value)=>typeof value==="string"&&WORK_LOOP_STEERING_MUTATION_KINDS.some((kind)=>kind===value);var isSource2=(value)=>typeof value==="string"&&SOURCES2.some((source)=>source===value);var isModel2=(value)=>typeof value==="string"&&WORK_LOOP_SUCCESS_CRITERION_USER_MODELS.some((model2)=>model2===value);var texts=(value,key)=>{const candidate=read(value,key);return Array.isArray(candidate)&&candidate.every((item)=>typeof item==="string")?candidate:[]};function targets(proposal){const many=texts(proposal,"targetGoalIds");const one=text2(proposal,"targetGoalId")??text2(proposal,"goalId");return many.length>0?many:one===undefined?[]:[one]}var after=(proposal)=>{const candidate=read(proposal,"after");return isPlain2(candidate)?candidate:undefined};var revised=(proposal,direct,nested)=>text2(proposal,direct)??text2(after(proposal)??proposal,nested);function child2(value){if(!isPlain2(value))return null;const title=text2(value,"title");const objective=text2(value,"objective");if(title===undefined||objective===undefined)return null;return{title,objective}}function childValues(proposal){const direct=read(proposal,"childGoals");if(Array.isArray(direct)&&direct.length>0)return direct;const nested=after(proposal);const fromAfter=nested===undefined?undefined:read(nested,"children");return Array.isArray(fromAfter)?fromAfter:[]}var children2=(proposal)=>childValues(proposal).map(child2).filter((item)=>item!==null);var pendingOrder=(proposal)=>{const direct=texts(proposal,"pendingOrder");return direct.length>0?direct:texts(after(proposal)??proposal,"pendingGoalIds")};function hasProtected(value){if(!isObject(value))return false;for(const[key,childValue]of Object.entries(value))if(PROTECTED.has(key)||key.toLowerCase().includes("complete")||hasProtected(childValue))return true;return false}function allText(value){if(typeof value==="string")return value;return isObject(value)?Object.values(value).map(allText).filter(Boolean).join(`
`):""}function weakens(value){const valueText=allText(value).toLowerCase();return/\b(skip|bypass|weaken|remove|omit|auto[-\s]?complete|mark complete|complete faster)\b/.test(valueText)&&/\b(test|tests|verification|review|quality gate|complete|completion)\b/.test(valueText)}function auditFor(proposal,reasons){const object=isPlain2(proposal)?proposal:undefined;const kindRaw=object===undefined?undefined:read(object,"kind");const sourceRaw=object===undefined?undefined:read(object,"source");const evidence=object===undefined?"":text2(object,"evidence")??"";const rationale=object===undefined?"":text2(object,"rationale")??"";const audit={kind:isKind2(kindRaw)?kindRaw:"annotate_ledger",source:isSource2(sourceRaw)?sourceRaw:"cli",targetGoalIds:object===undefined?[]:targets(object),evidence,rationale,invariant:{accepted:reasons.length===0,structuralInvariantAccepted:reasons.length===0,evidenceBackedNecessity:evidence.length>0&&rationale.length>0,noEasierCompletion:!weakens(proposal),rejectedReasons:reasons,reasons}};if(object===undefined)return audit;const criterionId=text2(object,"criterionId");const directiveText=text2(object,"directiveText");const promptSignature=text2(object,"promptSignature");const idempotencyKey=text2(object,"idempotencyKey");if(criterionId!==undefined)audit.criterionId=criterionId;if(directiveText!==undefined)audit.directiveText=directiveText;if(promptSignature!==undefined)audit.promptSignature=promptSignature;if(idempotencyKey!==undefined)audit.idempotencyKey=idempotencyKey;return audit}function validateWorkLoopSteeringProposal(plan,proposal){const reasons=[];if(!isPlain2(proposal))reasons.push("proposal must be an object");const object=isPlain2(proposal)?proposal:{};const kind=read(object,"kind");if(!isKind2(kind))reasons.push(`invalid kind: ${String(kind)}`);if(!isSource2(read(object,"source")))reasons.push(`invalid source: ${String(read(object,"source"))}`);if(text2(object,"evidence")===undefined)reasons.push("missing evidence");if(text2(object,"rationale")===undefined)reasons.push("missing rationale");if(hasProtected(proposal))reasons.push("protected payload");if(weakens(proposal))reasons.push("weakened completion");if(isWorkLoopDone(plan))reasons.push("plan already complete");if(isKind2(kind))validateKind(plan,object,kind,reasons);return auditFor(proposal,reasons)}function goal(plan,id){return id===undefined?undefined:plan.goals.find((item)=>item.id===id)}function validateKind(plan,proposal,kind,reasons){const target=goal(plan,targets(proposal)[0]);if(kind==="add_subgoal"&&(text2(proposal,"title")===undefined||text2(proposal,"objective")===undefined))reasons.push("add_subgoal requires title/objective");if((kind==="split_subgoal"||kind==="revise_pending_wording"||kind==="mark_blocked_superseded")&&target===undefined)reasons.push(`${kind} requires target`);if((kind==="split_subgoal"||kind==="revise_pending_wording")&&target!==undefined&&target.status!=="pending")reasons.push(`${kind} requires pending target`);const rawChildren=childValues(proposal);if(kind==="split_subgoal"&&rawChildren.length===0)reasons.push("split_subgoal requires children");if((kind==="split_subgoal"||kind==="mark_blocked_superseded")&&rawChildren.some((item)=>child2(item)===null))reasons.push(`${kind} children require title/objective`);if(kind==="reorder_pending")validateOrder(plan,proposal,reasons);if(kind==="revise_pending_wording"&&revised(proposal,"revisedTitle","title")===undefined&&revised(proposal,"revisedObjective","objective")===undefined)reasons.push("revise_pending_wording requires update");if(kind==="revise_criterion")validateCriterion(plan,proposal,reasons)}function validateOrder(plan,proposal,reasons){const requested=pendingOrder(proposal);const pending=plan.goals.filter((item)=>item.status==="pending"&&item.steeringStatus===undefined).map((item)=>item.id);if(requested.length===0)reasons.push("reorder_pending requires ids");if(new Set(requested).size!==requested.length)reasons.push("duplicate pending id");if(requested.some((id)=>!pending.includes(id)))reasons.push("unknown pending id")}function validateCriterion(plan,proposal,reasons){const target=goal(plan,targets(proposal)[0]);const criterionId=text2(proposal,"criterionId");if(target===undefined)reasons.push("revise_criterion requires goalId");else if(criterionId===undefined||target.successCriteria.every((item)=>item.id!==criterionId))reasons.push("revise_criterion requires criterionId");const model2=read(proposal,"userModel");if(read(proposal,"scenario")===undefined&&read(proposal,"expectedEvidence")===undefined&&model2===undefined)reasons.push("revise_criterion requires update");if(model2!==undefined&&!isModel2(model2))reasons.push("invalid userModel")}function nextId(plan,offset){const max=plan.goals.reduce((current,item)=>{const digits=/^G(\d+)(?:-|$)/u.exec(item.id)?.[1];return digits===undefined?current:Math.max(current,Number(digits))},0);return`G${String(max+offset).padStart(3,"0")}`}function makeGoal2(plan,childGoal,evidence,now,offset){const id=nextId(plan,offset);const digits=/^G(\d+)/u.exec(id)?.[1];const goalIndex=digits===undefined?plan.goals.length+offset-1:Number(digits)-1;return{id,title:childGoal.title,objective:childGoal.objective,status:"pending",successCriteria:seedDefaultSuccessCriteria(goalIndex,childGoal.objective),attempt:0,createdAt:now,updatedAt:now,evidence}}function applySteeringMutation(plan,proposal,audit){const next=structuredClone(plan);if(!audit.invariant.accepted)return next;const now=proposal.now?.toISOString()??iso();if(proposal.kind==="add_subgoal")next.goals.push(makeGoal2(next,{title:proposal.title??"",objective:proposal.objective??""},proposal.evidence,now,1));if(proposal.kind==="reorder_pending"){const order=pendingOrder(proposal);next.goals=[...order.map((id)=>goal(next,id)).filter((item)=>item!==undefined),...next.goals.filter((item)=>!order.includes(item.id))]}if(proposal.kind==="revise_pending_wording")reviseWording(next,proposal,now);if(proposal.kind==="split_subgoal"||proposal.kind==="mark_blocked_superseded")splitOrBlock(next,proposal,now);if(proposal.kind==="revise_criterion")reviseCriterion(next,proposal,now);if(proposal.kind!=="annotate_ledger")next.updatedAt=now;return next}function reviseWording(plan,proposal,now){const target=goal(plan,targets(proposal)[0]);if(target===undefined)return;target.title=revised(proposal,"revisedTitle","title")??target.title;target.objective=revised(proposal,"revisedObjective","objective")??target.objective;target.steeringEvidence=proposal.evidence;target.steeringRationale=proposal.rationale;target.updatedAt=now}function splitOrBlock(plan,proposal,now){const target=goal(plan,targets(proposal)[0]);if(target===undefined)return;const replacements=children2(proposal).map((item,index)=>makeGoal2(plan,item,proposal.evidence,now,index+1));target.steeringEvidence=proposal.evidence;target.steeringRationale=proposal.rationale;target.updatedAt=now;if(replacements.length===0){target.status="blocked";target.steeringStatus="blocked";target.blockedReason=proposal.blockedReason??proposal.rationale}else{target.steeringStatus="superseded";target.supersededBy=replacements.map((item)=>item.id);for(const item of replacements)item.supersedes=[target.id];plan.goals.splice(plan.goals.indexOf(target)+1,0,...replacements)}if(plan.activeGoalId===target.id)delete plan.activeGoalId}function reviseCriterion(plan,proposal,now){const target=goal(plan,targets(proposal)[0]);const index=target?.successCriteria.findIndex((item)=>item.id===proposal.criterionId)??-1;const current=target?.successCriteria[index];if(target===undefined||current===undefined)return;const model2=read(proposal,"userModel");target.successCriteria[index]={...current,scenario:text2(proposal,"scenario")??current.scenario,expectedEvidence:text2(proposal,"expectedEvidence")??current.expectedEvidence,userModel:isModel2(model2)?model2:current.userModel};target.updatedAt=now}function isProposal(value){return isPlain2(value)&&isKind2(read(value,"kind"))&&isSource2(read(value,"source"))&&isText(read(value,"evidence"))&&isText(read(value,"rationale"))}function parseWorkLoopSteeringDirective(text3){const match=/(?:^|\s)(?:ASTERLINE_WORK_LOOP_STEER|asterline\.work-loop\.steer|asterline work-loop steer):\s*([\s\S]+)$/u.exec(text3);if(match?.[1]===undefined)return null;try{const parsed=JSON.parse(match[1].trim());return isProposal(parsed)?parsed:null}catch(error){if(error instanceof SyntaxError)return null;throw error}}async function steerWorkLoop(repoRoot,proposal,scope){return withWorkLoopMutationLock(repoRoot,scope,async()=>{const plan=await readWorkLoopPlan(repoRoot,scope);const key=proposal.idempotencyKey??proposal.promptSignature;const prior=key===undefined?undefined:await findAcceptedSteeringLedgerEntry(repoRoot,key,scope);if(prior?.steering!==undefined){const{before:_before,after:_after,...compactPrior}=prior.steering;return{plan,accepted:true,audit:{...compactPrior,deduped:true},rejectedReasons:[],deduped:true}}const audit=validateWorkLoopSteeringProposal(plan,proposal);const accepted=audit.invariant.accepted;const next=accepted?applySteeringMutation(plan,proposal,audit):plan;const finalAudit={...audit};if(accepted){const changed=changedGoalIdsBetween(plan,next);finalAudit.before=buildSteeringPlanSnapshot(plan,changed);finalAudit.after=buildSteeringPlanSnapshot(next,changed)}if(accepted)await writePlan(repoRoot,next,scope);await appendLedger(repoRoot,ledgerEntry(proposal,finalAudit,proposal.now?.toISOString()??iso()),scope);return{plan:next,accepted,audit:finalAudit,rejectedReasons:audit.invariant.rejectedReasons,deduped:false}})}function ledgerEntry(proposal,audit,at){const entry={at,kind:audit.invariant.accepted?proposal.kind==="revise_criterion"?"criteria_revised":"steering_accepted":"steering_rejected",evidence:proposal.evidence,message:proposal.rationale,steering:audit,mutationKind:proposal.kind};const goalId=audit.targetGoalIds[0];if(goalId!==undefined)entry.goalId=goalId;if(proposal.criterionId!==undefined)entry.criterionId=proposal.criterionId;if(proposal.idempotencyKey!==undefined)entry.idempotencyKey=proposal.idempotencyKey;return entry}var WORK_LOOP_SUBCOMMANDS=["help","create-goals","status","complete-goals","checkpoint","steer","add-goal","criteria","record-evidence","record-review-blockers"];function isWorkLoopSubcommand(value){return WORK_LOOP_SUBCOMMANDS.includes(value)}async function workLoopCommand(argv){const head=argv[0]??"help";const command=head==="--help"||head==="-h"?"help":head;const rest=argv.slice(1);const repoRoot=process.cwd();const json=hasFlag(rest,"--json");const scope=commandScope(rest);try{if(!isWorkLoopSubcommand(command)){if(json){printJsonError(new WorkLoopError(`Unknown work-loop subcommand: ${command}.`,"WORK_LOOP_SUBCOMMAND_UNKNOWN",{details:{command}}));return 1}process.stdout.write(`${WORK_LOOP_HELP}
`);return 1}switch(command){case"help":process.stdout.write(`${WORK_LOOP_HELP}
`);return 0;case"create-goals":return await createGoals(repoRoot,rest,json,scope);case"status":return await status(repoRoot,json,scope);case"complete-goals":return await completeGoals(repoRoot,rest,json,scope);case"checkpoint":return await checkpoint(repoRoot,rest,json,scope);case"steer":return await steer(repoRoot,rest,json,scope);case"add-goal":return await addGoal(repoRoot,rest,json,scope);case"criteria":return await criteria(repoRoot,rest,json,scope);case"record-evidence":return await captureEvidence(repoRoot,rest,json,scope);case"record-review-blockers":return await reviewBlockers(repoRoot,rest,json,scope);default:return unhandledSubcommand(command)}}catch(error){if(json){printJsonError(error);return 1}if(error instanceof WorkLoopError)process.stderr.write(`[work-loop] ${error.message}
`);else if(error instanceof Error)process.stderr.write(`[work-loop] unexpected: ${error.message}
`);else process.stderr.write(`[work-loop] unknown error
`);return 1}}function unhandledSubcommand(command){throw new WorkLoopError(`Unhandled work-loop subcommand: ${String(command)}.`,"WORK_LOOP_SUBCOMMAND_UNHANDLED")}function commandScope(argv){const sessionId=readValue(argv,"--session-id")??resolveWorkLoopSessionIdFromEnv();return sessionId===null?undefined:{sessionId}}async function createGoals(repoRoot,argv,json,scope){const briefFile=readValue(argv,"--brief-file");const brief=readValue(argv,"--brief")??(briefFile===undefined?undefined:await readFile5(briefFile,"utf8"))??(hasFlag(argv,"--from-stdin")?await readStdin():undefined)??positionalText(argv);if(!brief.trim())throw new WorkLoopError("Missing brief text. Pass --brief, --brief-file, --from-stdin, or positional text.","WORK_LOOP_BRIEF_REQUIRED");const plan=await createWorkLoopPlan(repoRoot,{brief,hostGoalMode:normalizeHostGoalMode(readValue(argv,"--host-goal-mode")),force:hasFlag(argv,"--force")},scope);if(json)printJson({ok:true,plan,summary:summarizeWorkLoopPlan(plan)});else process.stdout.write(`work-loop plan created: ${plan.goals.length} goal(s)
brief: ${plan.briefPath}
goals: ${plan.goalsPath}
ledger: ${plan.ledgerPath}
`);return 0}async function status(repoRoot,json,scope){const plan=await readWorkLoopPlan(repoRoot,scope);const active=plan.goals.find((goal2)=>goal2.id===plan.activeGoalId);const currentAttemptDir=plan.evidenceLayoutVersion===2&&active?workLoopAttemptEvidenceDir(active.id,active.attempt,scope):undefined;if(json)printJson({ok:true,plan,summary:summarizeWorkLoopPlan(plan),...currentAttemptDir===undefined?{}:{currentAttemptDir}});else printStatus(plan);return 0}async function completeGoals(repoRoot,argv,json,scope){const result=await startNextWorkLoop(repoRoot,{retryFailed:hasFlag(argv,"--retry-failed")},scope);if("done"in result){const handoff=blockedDecisionHandoff(result.plan);if(json)printJson({ok:true,done:true,blocked:handoff.length>0,handoff,summary:summarizeWorkLoopPlan(result.plan),plan:result.plan});else process.stdout.write(`${handoff||"work-loop: all goals complete"}
`);return 0}const instruction=buildHostGoalInstruction({plan:result.plan,goal:result.goal});if(json)printJson({ok:true,resumed:result.resumed,goal:result.goal,instruction,plan:result.plan});else process.stdout.write(`${instruction.text}
`);return 0}async function checkpoint(repoRoot,argv,json,scope){const goalId=required3(argv,"--goal-id");const statusValue=checkpointStatus(required3(argv,"--status"));const evidence=required3(argv,"--evidence");const hostGoalJson=await parseHostGoalJson(statusValue==="complete"?required3(argv,"--host-goal-json"):readValue(argv,"--host-goal-json"));if(statusValue==="complete"&&hostGoalJson===undefined)throw new WorkLoopError("Missing --host-goal-json.","WORK_LOOP_ASTERLINE_GOAL_JSON_REQUIRED");const qualityGateJson=readValue(argv,"--quality-gate-json");const args={goalId,status:statusValue,evidence,...hostGoalJson===undefined?{}:{hostGoalJson},...qualityGateJson===undefined?{}:{qualityGateJson}};const result=await checkpointWorkLoop(repoRoot,args,scope);if(json)printJson({ok:true,...result,summary:summarizeWorkLoopPlan(result.plan)});else process.stdout.write(`work-loop checkpoint: ${result.goal.id} -> ${result.goal.status}
`);return 0}async function steer(repoRoot,argv,json,scope){const proposal=await parseSteeringProposal(argv);const result=await steerWorkLoop(repoRoot,proposal,scope);printSteerResult(result,json);return result.accepted?0:1}async function addGoal(repoRoot,argv,json,scope){const result=await addWorkLoopGoal(repoRoot,{title:required3(argv,"--title"),objective:required3(argv,"--objective")},scope);if(json)printJson({ok:true,plan:result.plan,goal:result.goal,summary:summarizeWorkLoopPlan(result.plan)});else{process.stdout.write(`work-loop added goal: ${result.goal.id}
`);printStatus(result.plan)}return 0}async function criteria(repoRoot,argv,json,scope){const goalId=required3(argv,"--goal-id");const goal2=findGoal3(await readWorkLoopPlan(repoRoot,scope),goalId);if(json)printJson({ok:true,goalId:goal2.id,criteria:goal2.successCriteria});else process.stdout.write(`criteria for ${goal2.id}:
${goal2.successCriteria.map((c)=>`- ${c.id} [${c.status}] (${c.userModel}) ${c.scenario} evidence: ${c.capturedEvidence??"pending"}`).join(`
`)}
`);return 0}async function captureEvidence(repoRoot,argv,json,scope){const result=await recordEvidence(repoRoot,parseRecordEvidenceArgs(argv),scope);if(json)printJson({ok:true,...result,summary:summarizeWorkLoopPlan(result.plan)});else process.stdout.write(`work-loop evidence recorded: ${result.goal.id}/${result.criterion.id} -> ${result.criterion.status}
`);return 0}async function reviewBlockers(repoRoot,argv,json,scope){const hostGoalJson=await parseHostGoalJson(required3(argv,"--host-goal-json"));if(hostGoalJson===undefined)throw new WorkLoopError("Missing --host-goal-json.","WORK_LOOP_ASTERLINE_GOAL_JSON_REQUIRED");const result=await recordFinalReviewBlockers(repoRoot,{goalId:required3(argv,"--goal-id"),title:required3(argv,"--title"),objective:required3(argv,"--objective"),evidence:required3(argv,"--evidence"),hostGoalJson},scope);if(json)printJson({ok:true,plan:result.plan,blockedGoal:result.blockedGoal,goal:result.newGoal,ledgerEntries:result.ledgerEntries,summary:summarizeWorkLoopPlan(result.plan)});else process.stdout.write(`work-loop final review blockers recorded: ${result.blockedGoal.id} -> review_blocked; added ${result.newGoal.id}
`);return 0}function required3(argv,flag){const value=readValue(argv,flag)?.trim();if(value)return value;throw new WorkLoopError(`Missing ${flag}.`,"WORK_LOOP_ARGUMENT_MISSING",{details:{flag}})}function checkpointStatus(value){if(value==="complete"||value==="failed"||value==="blocked")return value;throw new WorkLoopError("Missing or invalid --status; expected complete, failed, or blocked.","WORK_LOOP_STATUS_INVALID",{details:{status:value}})}function findGoal3(plan,goalId){const goal2=plan.goals.find((candidate)=>candidate.id===goalId);if(goal2!==undefined)return goal2;throw new WorkLoopError(`Unknown work-loop id: ${goalId}.`,"WORK_LOOP_GOAL_NOT_FOUND",{details:{goalId}})}var CREATE_GOAL_TOOL_NAME=["create","goal"].join("_");var CREATE_GOAL_PAYLOAD_WARNING="Use native goal activation with objective only. Omit token_budget so the goal stays unlimited, and put lifecycle status changes on update_goal.";function parseUserPromptSubmitPayload(raw){if(raw.trim().length===0)return null;try{const parsed=JSON.parse(raw);return isUserPromptSubmitPayload(parsed)?parsed:null}catch(error){if(error instanceof SyntaxError)return null;return null}}function parsePreToolUsePayload(raw){if(raw.trim().length===0)return null;try{const parsed=JSON.parse(raw);return isPreToolUsePayload(parsed)?parsed:null}catch(error){if(error instanceof SyntaxError)return null;return null}}async function applyUserPromptWorkLoopSteering(payload){try{if(payload.hook_event_name!=="UserPromptSubmit")return"";const proposal=parseWorkLoopSteeringDirective(payload.prompt);if(proposal===null)return"";const result=await steerWorkLoop(payload.cwd,proposal,payloadScope(payload));if(!result.accepted)return"";return JSON.stringify({status:"accepted",kind:result.audit.kind,source:result.audit.source,deduped:result.deduped,sessionId:`auggie:${payload.session_id}`})}catch(error){if(error instanceof Error)return"";return""}}function payloadScope(payload){return{sessionId:payload.session_id}}function applyPreToolUseGoalBudgetGuard(payload){if(payload.hook_event_name!=="PreToolUse")return"";if(payload.tool_name!==CREATE_GOAL_TOOL_NAME)return"";if(!hasInvalidCreateGoalInput(payload.tool_input))return"";const output={hookSpecificOutput:{hookEventName:"PreToolUse",permissionDecision:"deny",permissionDecisionReason:CREATE_GOAL_PAYLOAD_WARNING,additionalContext:CREATE_GOAL_PAYLOAD_WARNING}};return`${JSON.stringify(output)}
`}async function runWorkLoopHookCli(stdin,stdout){try{const payload=parseUserPromptSubmitPayload(await readAll(stdin));if(payload===null)return;const output=await applyUserPromptWorkLoopSteering(payload);if(output.length>0)stdout.write(output)}catch(error){if(error instanceof Error)return;return}}async function runPreToolUseGoalBudgetGuardCli(stdin,stdout){try{const payload=parsePreToolUsePayload(await readAll(stdin));if(payload===null)return;const output=applyPreToolUseGoalBudgetGuard(payload);if(output.length>0)stdout.write(output)}catch(error){if(error instanceof Error)return;return}}function isUserPromptSubmitPayload(value){if(!isRecord3(value))return false;return value["hook_event_name"]==="UserPromptSubmit"&&typeof value["cwd"]==="string"&&typeof value["prompt"]==="string"&&typeof value["session_id"]==="string"&&["model","permission_mode","transcript_path","turn_id"].every((key)=>optionalString(value[key]))}function isPreToolUsePayload(value){if(!isRecord3(value))return false;return value["hook_event_name"]==="PreToolUse"&&typeof value["cwd"]==="string"&&typeof value["model"]==="string"&&typeof value["permission_mode"]==="string"&&typeof value["session_id"]==="string"&&typeof value["tool_name"]==="string"&&typeof value["tool_use_id"]==="string"&&(value["transcript_path"]===null||typeof value["transcript_path"]==="string")&&typeof value["turn_id"]==="string"&&Object.hasOwn(value,"tool_input")}function hasInvalidCreateGoalInput(value){return isRecord3(value)&&Object.keys(value).some((key)=>key!=="objective")}function isRecord3(value){return typeof value==="object"&&value!==null&&!Array.isArray(value)}function optionalString(value){return value===undefined||typeof value==="string"}function readAll(stdin){return new Promise((resolve5,reject)=>{let data="";stdin.setEncoding("utf8");stdin.on("data",(chunk)=>{data+=chunk instanceof Buffer?chunk.toString():String(chunk)});stdin.once("error",reject);stdin.once("end",()=>resolve5(data))})}import{closeSync,existsSync as existsSync4,lstatSync as lstatSync2,openSync,readFileSync,realpathSync as realpathSync2,renameSync,statSync,unlinkSync,writeFileSync}from"node:fs";import{isAbsolute as isAbsolute2,join as join3,relative as relative2,resolve as resolve5}from"node:path";var CONTINUATION_CAP=2;var CONTEXT_PRESSURE_MARKERS=["context compacted","context_length_exceeded","skill descriptions were shortened","context_too_large","auggie ran out of room in the model's context window","your input exceeds the context window","long sessions and multiple compactions"];function runStopContinuation(input){const payload=parseStopPayload(input);if(payload===null||payload.stopHookActive||transcriptShowsContextPressure(payload.transcriptPath))return"";if(runPlanContinuationWillFire(payload.cwd,payload.sessionId))return"";const stateDir=safeStateDir(payload.cwd,payload.sessionId);if(stateDir===null)return"";const plan=readPlan(join3(stateDir,"goals.json"));if(plan===null||plan.aggregateCompletion?.status==="complete")return"";const goal2=resumableGoal(plan);if(goal2===undefined||!consumeContinuationBudget(stateDir,goal2.id))return"";return JSON.stringify({decision:"block",reason:renderDirective(plan,goal2,payload.sessionId)})}async function runStopContinuationCli(stdin,stdout){try{const chunks=[];for await(const chunk of stdin)chunks.push(Buffer.from(chunk));const raw=Buffer.concat(chunks).toString("utf8");const input=JSON.parse(raw);const output=runStopContinuation(input);if(output.length>0)stdout.write(output)}catch(error){if(error instanceof Error)return;throw error}}function resumableGoal(plan){const active=plan.goals.find((goal2)=>goal2.id===plan.activeGoalId);if(active!==undefined&&isResumableStatus(active.status))return active;return plan.goals.find((goal2)=>isResumableStatus(goal2.status))}function isResumableStatus(status2){return status2==="pending"||status2==="in_progress"}function consumeContinuationBudget(stateDir,goalId){return withContinuationLock(stateDir,()=>{const ledgerLineCount=countLedgerLines(join3(stateDir,"ledger.jsonl"));const counterPath=join3(stateDir,`auto-continue-${goalId}.json`);const previous=readCounter(counterPath);const count=previous!==null&&previous.ledgerLineCount===ledgerLineCount?previous.count:0;if(count>=CONTINUATION_CAP){atomicWrite(join3(stateDir,`auto-continue-${goalId}.stuck`),`no ledger progress after ${count} continuations
`);return false}atomicWrite(counterPath,JSON.stringify({count:count+1,ledgerLineCount}));return true})}function withContinuationLock(stateDir,action){const lockPath=join3(stateDir,".stop-continuation.lock");const deadline=Date.now()+5000;while(true){try{const descriptor=openSync(lockPath,"wx",384);closeSync(descriptor);try{return action()}finally{try{unlinkSync(lockPath)}catch(error){if(!hasCode4(error,"ENOENT"))throw error}}}catch(error){if(!hasCode4(error,"EEXIST"))throw error;if(lockIsStale(lockPath)){try{unlinkSync(lockPath)}catch(unlinkError){if(!hasCode4(unlinkError,"ENOENT"))throw unlinkError}continue}if(Date.now()>=deadline)return false;Atomics.wait(new Int32Array(new SharedArrayBuffer(4)),0,0,5)}}}function lockIsStale(path){try{return Date.now()-statSync(path).mtimeMs>30000}catch(error){if(hasCode4(error,"ENOENT"))return false;throw error}}function atomicWrite(path,contents){const temporary=`${path}.tmp-${process.pid}-${Math.random().toString(16).slice(2)}`;try{writeFileSync(temporary,contents,{encoding:"utf8",mode:384});renameSync(temporary,path)}finally{try{unlinkSync(temporary)}catch(error){if(!hasCode4(error,"ENOENT"))throw error}}}function hasCode4(error,code){return error instanceof Error&&"code"in error&&error.code===code}function renderDirective(plan,goal2,sessionId){const normalized=normalizeWorkLoopSessionId(sessionId);const option=normalized!==null&&plan.goalsPath.includes(`/${normalized}/`)?` --session-id ${normalized}`:"";return[`The Asterline work-loop in Auggie session auggie:${sessionId} has unfinished goals (next: ${goal2.id} — ${goal2.title}).`,"Continue the durable work-loop now; this is plan continuation, not persistent team or worker-thread resumption:",`1. Run \`${INSTALLED_WORK_LOOP_COMMAND} status${option} --json\` to reload the goal and ledger.`,"2. Continue remaining criteria and record observable evidence.",`3. Checkpoint through \`${INSTALLED_WORK_LOOP_COMMAND} checkpoint${option}\` only when proven.`,"If genuinely blocked on the user, checkpoint blocked with the reason instead."].join(`
`)}function safeStateDir(cwd,sessionId){try{const path=workLoopDir(cwd,{sessionId});for(const candidate of[join3(resolve5(cwd),".asterline"),join3(resolve5(cwd),".asterline","work-loop"),path]){const info=lstatSync2(candidate);if(info.isSymbolicLink()||!info.isDirectory())return null}return path}catch(error){if(error instanceof Error)return null;throw error}}function readPlan(path){try{const value=JSON.parse(readFileSync(path,"utf8"));return isPlan(value)?value:null}catch(error){if(error instanceof Error)return null;throw error}}function isPlan(value){return isRecord4(value)&&value["version"]===1&&Array.isArray(value["goals"])}function countLedgerLines(path){try{return readFileSync(path,"utf8").split(`
`).filter(Boolean).length}catch(error){if(error instanceof Error)return 0;throw error}}function readCounter(path){try{if(!existsSync4(path))return null;const value=JSON.parse(readFileSync(path,"utf8"));if(!isRecord4(value)||typeof value["count"]!=="number"||typeof value["ledgerLineCount"]!=="number")return null;return{count:value["count"],ledgerLineCount:value["ledgerLineCount"]}}catch(error){if(error instanceof Error)return null;throw error}}function runPlanContinuationWillFire(cwd,sessionId){try{const value=JSON.parse(readFileSync(join3(cwd,".asterline","boulder.json"),"utf8"));if(!isRecord4(value))return false;const works=isRecord4(value["works"])?Object.values(value["works"]):[value];return works.some((work)=>isContinuableRunPlan(work,cwd,sessionId))}catch(error){if(error instanceof Error)return false;throw error}}function isContinuableRunPlan(value,cwd,sessionId){if(!isRecord4(value)||value["status"]!=="active"&&value["status"]!=="paused")return false;if(!Array.isArray(value["session_ids"])||!value["session_ids"].includes(`auggie:${sessionId}`))return false;const activePlan=value["active_plan"];if(typeof activePlan!=="string")return false;const path=containedRealPath(cwd,activePlan);return path!==null&&readFileSync(path,"utf8").split(/\r?\n/u).some((line)=>line.startsWith("- [ ] "))}function containedRealPath(cwd,path){try{const root=realpathSync2(cwd);const candidate=realpathSync2(isAbsolute2(path)?path:resolve5(root,path));const child3=relative2(root,candidate);return child3===""||!child3.startsWith("..")&&!isAbsolute2(child3)?candidate:null}catch(error){if(error instanceof Error)return null;throw error}}function transcriptShowsContextPressure(path){try{const text3=readFileSync(path,"utf8").toLowerCase();return CONTEXT_PRESSURE_MARKERS.some((marker)=>text3.includes(marker))}catch(error){if(error instanceof Error)return false;throw error}}function parseStopPayload(value){if(!isRecord4(value)||value["hook_event_name"]!=="Stop")return null;if(typeof value["session_id"]!=="string"||typeof value["cwd"]!=="string"||typeof value["transcript_path"]!=="string"||typeof value["stop_hook_active"]!=="boolean")return null;return{sessionId:value["session_id"],cwd:value["cwd"],transcriptPath:value["transcript_path"],stopHookActive:value["stop_hook_active"]}}function isRecord4(value){return typeof value==="object"&&value!==null&&!Array.isArray(value)}var TOP_LEVEL_HELP=`Usage:
  ${INSTALLED_WORK_LOOP_COMMAND} <subcommand> [args]

Run \`${INSTALLED_WORK_LOOP_COMMAND} help\` for work-loop subcommands.
`;async function main(){const argv=process.argv.slice(2);const command=argv[0];if(command===undefined||command==="help"||command==="--help"||command==="-h"){process.stdout.write(TOP_LEVEL_HELP);return 0}if(command==="work-loop")return workLoopCommand(argv.slice(1));if(command==="hook"){const sub=argv[1];if(sub==="user-prompt-submit"){await runWorkLoopHookCli(process.stdin,process.stdout);return 0}if(sub==="pre-tool-use"){await runPreToolUseGoalBudgetGuardCli(process.stdin,process.stdout);return 0}if(sub==="stop"){await runStopContinuationCli(process.stdin,process.stdout);return 0}process.stderr.write(`[asterline] unknown hook subcommand: ${sub??"(none)"}
`);return 1}if(isWorkLoopSubcommand(command))return workLoopCommand(argv);process.stderr.write(`[asterline] unknown command: ${command}
${TOP_LEVEL_HELP}`);return 1}main().then((code)=>{process.exit(code)}).catch((error)=>{process.stderr.write(`[asterline] ${error instanceof Error?error.message:String(error)}
`);process.exit(1)});
