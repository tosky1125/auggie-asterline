{
	"name": "@sisyphuslabs/asterline-bootstrap",
	"version": "4.10.0",
	"description": "Asterline SessionStart bootstrap component that provisions Asterline runtime dependencies from a detached worker.",
	"type": "module",
	"private": true,
	"files": [
		"dist",
		"hooks",
		"scripts",
		"manifests"
	],
	"scripts": {
		"build": "node scripts/build.mjs",
		"test": "bun test test/*.test.ts"
	},
	"engines": {
		"node": ">=20.0.0"
	}
}
