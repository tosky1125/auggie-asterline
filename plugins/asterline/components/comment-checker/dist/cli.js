#!/usr/bin/env node
import{stdin as processStdin,stdout as processStdout}from"node:process";var MAX_PAYLOAD_UNITS=1048576;var MAX_NESTING_DEPTH=64;var DANGEROUS_KEYS=new Set(["__proto__","prototype","constructor"]);class AuggiePayloadError extends Error{path;name="AuggiePayloadError";constructor(message,path="$"){super(`${path}: ${message}`);this.path=path}}function parseJsonValue(value,frame){if(frame.depth>MAX_NESTING_DEPTH)throw new AuggiePayloadError("payload exceeds nesting limit",frame.path);frame.budget.remaining-=1;if(frame.budget.remaining<0)throw new AuggiePayloadError("payload exceeds size limit",frame.path);if(value===null||typeof value==="boolean")return value;if(typeof value==="string"){frame.budget.remaining-=value.length;if(frame.budget.remaining<0)throw new AuggiePayloadError("payload exceeds size limit",frame.path);return value}if(typeof value==="number"){if(!Number.isFinite(value))throw new AuggiePayloadError("number must be finite",frame.path);return value}if(Array.isArray(value)){return value.map((item,index)=>parseJsonValue(item,{path:`${frame.path}[${index}]`,depth:frame.depth+1,budget:frame.budget}))}if(!isPlainRecord(value))throw new AuggiePayloadError("expected a plain JSON object",frame.path);const parsed={};for(const[key,item]of Object.entries(value)){const path=`${frame.path}.${key}`;if(DANGEROUS_KEYS.has(key))throw new AuggiePayloadError("dangerous object key",path);frame.budget.remaining-=key.length;parsed[key]=parseJsonValue(item,{path,depth:frame.depth+1,budget:frame.budget})}return parsed}function parseJsonEnvelope(raw){if(typeof raw==="string"&&raw.length>MAX_PAYLOAD_UNITS){throw new AuggiePayloadError("payload exceeds size limit")}let decoded=raw;if(typeof raw==="string"){try{decoded=JSON.parse(raw)}catch(error){if(error instanceof SyntaxError)throw new AuggiePayloadError("invalid JSON");throw error}}const value=parseJsonValue(decoded,{path:"$",depth:0,budget:{remaining:MAX_PAYLOAD_UNITS}});if(!isJsonObject(value))throw new AuggiePayloadError("expected an object envelope");return value}function isPlainRecord(value){if(typeof value!=="object"||value===null||Array.isArray(value))return false;const prototype=Object.getPrototypeOf(value);return prototype===Object.prototype||prototype===null}function isJsonObject(value){return typeof value==="object"&&value!==null&&!Array.isArray(value)}var CANCELLATION=/\b(cancelled|canceled|interrupted)\b/i;function requiredString(record,key){const value=record[key];if(typeof value!=="string"||value.length===0){throw new AuggiePayloadError("expected a non-empty string",`$.${key}`)}return value}function requiredObject(record,key){const value=record[key];if(value===undefined||!isJsonObject(value))throw new AuggiePayloadError("expected an object",`$.${key}`);return value}function stringArray(record,key){const value=record[key];if(!Array.isArray(value)||value.some((item)=>typeof item!=="string"||item.length===0)){throw new AuggiePayloadError("expected an array of non-empty strings",`$.${key}`)}return value.filter((item)=>typeof item==="string")}function parseState(envelope,phase,tool){if(phase==="pre")return{kind:"pending"};if(!Object.hasOwn(envelope,"tool_output")||!Object.hasOwn(envelope,"tool_error")){throw new AuggiePayloadError("PostToolUse requires tool_output and tool_error")}const output=envelope["tool_output"];const error=envelope["tool_error"];if(error!==null){if(typeof error==="string"&&CANCELLATION.test(error))return{kind:"cancelled",reason:error};return{kind:"failed",...error===undefined?{}:{error}}}if(tool==="launch-process"&&typeof output==="string")return parseProcessState(output);if(isJsonObject(output)&&typeof output["status"]==="string"){const status=output["status"];if(status==="success"||status==="succeeded"||status==="completed")return{kind:"succeeded"};if(status==="failed"||status==="error")return{kind:"failed",error:output};if(status==="cancelled"||status==="canceled"||status==="interrupted"){return{kind:"cancelled",reason:output}}return{kind:"unknown",facts:output}}if(typeof output==="string")return{kind:"succeeded"};return{kind:"unknown",facts:output===undefined?null:output}}function parseProcessState(output){const exitText=tagContent(output,"return-code");const stdout=tagContent(output,"output");const stderr=tagContent(output,"error");const exitCode=exitText===undefined?undefined:Number(exitText);if(exitCode===undefined||!Number.isInteger(exitCode))return{kind:"unknown",facts:output};const details={exitCode,...stdout===undefined?{}:{stdout},...stderr===undefined?{}:{stderr}};return exitCode===0?{kind:"succeeded",...details}:{kind:"failed",...details}}function tagContent(output,tag){const startToken=`<${tag}>`;const endToken=`</${tag}>`;const start=output.indexOf(startToken);const end=output.indexOf(endToken,start+startToken.length);if(start<0||end<0)return;return output.slice(start+startToken.length,end).replace(/^\n|\n$/g,"")}function fileChangePaths(envelope){const changes=envelope["file_changes"];if(changes===undefined)return[];if(!Array.isArray(changes))throw new AuggiePayloadError("expected an array","$.file_changes");return changes.map((change,index)=>{if(!isJsonObject(change))throw new AuggiePayloadError("expected an object",`$.file_changes[${index}]`);return requiredString(change,"path")})}function patchPaths(patch){const paths=[];for(const line of patch.split(`
`)){const match=/^\*\*\* (?:Add|Update|Delete) File: (.+)$/.exec(line);const path=match?.[1];if(path!==undefined&&path.length>0)paths.push(path)}return paths}function parseEdits(input){const edits=[];const numbered=Object.keys(input).filter((key)=>/^(old_str|insert_line)_\d+$/.test(key)).map((key)=>Number(key.slice(key.lastIndexOf("_")+1))).filter((value,index,values)=>values.indexOf(value)===index).sort((left,right)=>left-right);if(Object.hasOwn(input,"old_str")||Object.hasOwn(input,"new_str")){edits.push(editPair(input["old_str"],input["new_str"],"plain"))}for(const index of numbered)edits.push(editPair(input[`old_str_${index}`],input[`new_str_${index}`],String(index)));if(edits.length===0)throw new AuggiePayloadError("expected edit text fields","$.tool_input");return edits}function editPair(oldText,newText,suffix){if(oldText!==undefined&&typeof oldText!=="string"){throw new AuggiePayloadError("expected a string",`$.tool_input.old_str_${suffix}`)}if(newText!==undefined&&typeof newText!=="string"){throw new AuggiePayloadError("expected a string",`$.tool_input.new_str_${suffix}`)}if(oldText===undefined&&newText===undefined){throw new AuggiePayloadError("missing edit text",`$.tool_input.${suffix}`)}return{...oldText===undefined?{}:{oldText},...newText===undefined?{}:{newText}}}function normalizeAuggieToolEvent(raw){const envelope=parseJsonEnvelope(raw);const eventName=requiredString(envelope,"hook_event_name");if(eventName!=="PreToolUse"&&eventName!=="PostToolUse"){throw new AuggiePayloadError("unsupported hook event","$.hook_event_name")}const phase=eventName==="PreToolUse"?"pre":"post";const tool=requiredString(envelope,"tool_name");const input=requiredObject(envelope,"tool_input");const isMcpTool=envelope["is_mcp_tool"];if(typeof isMcpTool!=="boolean"){throw new AuggiePayloadError("expected a boolean","$.is_mcp_tool")}const common={phase,conversationId:requiredString(envelope,"conversation_id"),workspaceRoots:stringArray(envelope,"workspace_roots"),isMcpTool,state:parseState(envelope,phase,tool),raw:envelope};const changedPaths=fileChangePaths(envelope);switch(tool){case"launch-process":{const cwdValue=input["cwd"];const waitValue=input["wait"];const maxWaitValue=input["max_wait_seconds"];if(cwdValue!==undefined&&(typeof cwdValue!=="string"||cwdValue.length===0)){throw new AuggiePayloadError("expected a non-empty string","$.tool_input.cwd")}if(waitValue!==undefined&&typeof waitValue!=="boolean"){throw new AuggiePayloadError("expected a boolean","$.tool_input.wait")}if(maxWaitValue!==undefined&&(typeof maxWaitValue!=="number"||!Number.isInteger(maxWaitValue)||maxWaitValue<0)){throw new AuggiePayloadError("expected a non-negative integer","$.tool_input.max_wait_seconds")}return{...common,tool,input:{command:requiredString(input,"command"),...cwdValue===undefined?{}:{cwd:cwdValue},...waitValue===undefined?{}:{wait:waitValue},...maxWaitValue===undefined?{}:{maxWaitSeconds:maxWaitValue}},affectedPaths:changedPaths}}case"apply_patch":{const patch=requiredString(input,"input");return{...common,tool,input:{patch},affectedPaths:[...new Set([...patchPaths(patch),...changedPaths])]}}case"str-replace-editor":{const path=requiredString(input,"path");return{...common,tool,input:{command:requiredString(input,"command"),path,edits:parseEdits(input)},affectedPaths:[...new Set([path,...changedPaths])]}}case"save-file":{const path=requiredString(input,"path");const content=input["file_content"];if(typeof content!=="string"){throw new AuggiePayloadError("expected a string","$.tool_input.file_content")}return{...common,tool,input:{path,content},affectedPaths:[...new Set([path,...changedPaths])]}}default:throw new AuggiePayloadError("unsupported tool name","$.tool_name")}}function normalizeAuggieToolEventFailOpen(raw){try{return normalizeAuggieToolEvent(raw)}catch(error){if(error instanceof AuggiePayloadError)return null;throw error}}function parseApplyPatchRequests(patch,sourceToolName="apply_patch"){const requests=[];let current;const flush=()=>{if(!current)return;if(current.operation==="add"){const content=joinPatchLines(current.newLines);if(content.length>0){requests.push({sourceToolName,toolName:"Write",filePath:current.filePath,toolInput:{file_path:current.filePath,content}})}}if(current.operation==="update"){const newString=joinPatchLines(current.newLines);if(newString.length>0){const filePath=current.movePath??current.filePath;requests.push({sourceToolName,toolName:"Edit",filePath,toolInput:{file_path:filePath,old_string:joinPatchLines(current.oldLines),new_string:newString}})}}current=undefined};for(const line of patch.split(/\r?\n/)){if(line==="*** Begin Patch"||line==="*** End Patch")continue;if(line.startsWith("*** Add File: ")){flush();current=makeAccumulator("add",line.slice("*** Add File: ".length).trim());continue}if(line.startsWith("*** Update File: ")){flush();current=makeAccumulator("update",line.slice("*** Update File: ".length).trim());continue}if(line.startsWith("*** Delete File: ")){flush();current=makeAccumulator("delete",line.slice("*** Delete File: ".length).trim());continue}if(line.startsWith("*** Move to: ")){if(current?.operation==="update")current.movePath=line.slice("*** Move to: ".length).trim();continue}if(!current)continue;if(line.startsWith("@@"))continue;if(current.operation==="add"){if(line.startsWith("+"))current.newLines.push(line.slice(1));continue}if(current.operation==="update"){if(line.startsWith("+"))current.newLines.push(line.slice(1));if(line.startsWith("-"))current.oldLines.push(line.slice(1))}}flush();return requests}function makeAccumulator(operation,filePath){return{operation,filePath,oldLines:[],newLines:[]}}function joinPatchLines(lines){return lines.length===0?"":`${lines.join(`
`)}
`}function toHookInput(request,context){return{session_id:context.sessionId,tool_name:request.toolName,transcript_path:context.transcriptPath??"",cwd:context.cwd,hook_event_name:"PostToolUse",tool_input:request.toolInput}}import{existsSync}from"node:fs";import{createRequire}from"node:module";import{dirname,join}from"node:path";import{spawn}from"node:child_process";var MAX_PROCESS_OUTPUT_BYTES=64*1024;var MAX_COMBINED_OUTPUT_BYTES=96*1024;var DEFAULT_TIMEOUT_MS=30000;var KILL_GRACE_MS=250;function appendOutput(output,chunk,state,streamLimit,combinedLimit){if(output.truncated)return true;const bytes=Buffer.from(chunk);const permitted=Math.max(0,Math.min(streamLimit-output.bytes,combinedLimit-state.combinedBytes));const accepted=Math.min(bytes.length,permitted);if(accepted>0)output.text+=bytes.subarray(0,accepted).toString("utf8");output.bytes+=accepted;state.combinedBytes+=accepted;if(accepted===bytes.length)return false;output.truncated=true;return true}function formatOutput(output,stream){return output.truncated?`${output.text}
[${stream} truncated after ${output.bytes} captured bytes]`:output.text}function positiveInteger(value,fallback){return Number.isFinite(value)&&value>0?Math.floor(value):fallback}function spawnProcess(command,args,stdin,maxOutputBytes=MAX_PROCESS_OUTPUT_BYTES,timeoutMs=DEFAULT_TIMEOUT_MS){return new Promise((resolve)=>{const streamLimit=positiveInteger(maxOutputBytes,MAX_PROCESS_OUTPUT_BYTES);const combinedLimit=Math.min(MAX_COMBINED_OUTPUT_BYTES,Math.floor(streamLimit*1.5));const deadline=positiveInteger(timeoutMs,DEFAULT_TIMEOUT_MS);const child=spawn(command,args,{stdio:["pipe","pipe","pipe"],shell:false});const output={stdout:{text:"",bytes:0,truncated:false},stderr:{text:"",bytes:0,truncated:false},combinedBytes:0};let settled=false;let aborting=false;let deadlineTimer;let forceTimer;let fallbackTimer;const clearTimers=()=>{if(deadlineTimer!==undefined)clearTimeout(deadlineTimer);if(forceTimer!==undefined)clearTimeout(forceTimer);if(fallbackTimer!==undefined)clearTimeout(fallbackTimer)};const onStdout=(chunk)=>{if(!aborting&&appendOutput(output.stdout,chunk,output,streamLimit,combinedLimit))abort()};const onStderr=(chunk)=>{if(!aborting&&appendOutput(output.stderr,chunk,output,streamLimit,combinedLimit))abort()};const onStdinError=()=>{return};const cleanup=()=>{clearTimers();child.stdout.off("data",onStdout);child.stderr.off("data",onStderr);child.stdin.off("error",onStdinError);child.off("error",onError);child.off("close",onClose)};const finish=(exitCode)=>{if(settled)return;settled=true;cleanup();child.stdin.destroy();resolve({exitCode:aborting?null:exitCode,stdout:formatOutput(output.stdout,"stdout"),stderr:formatOutput(output.stderr,"stderr")})};const killSafely=(signal)=>{try{child.kill(signal)}catch(error){if(!(error instanceof Error))throw error}};function abort(){if(aborting||settled)return;aborting=true;child.stdin.destroy();killSafely("SIGTERM");forceTimer=setTimeout(()=>{killSafely("SIGKILL");fallbackTimer=setTimeout(()=>finish(null),KILL_GRACE_MS)},KILL_GRACE_MS)}function onError(error){appendOutput(output.stderr,error.message,output,streamLimit,combinedLimit);abort()}function onClose(exitCode){finish(exitCode)}child.stdout.setEncoding("utf8");child.stderr.setEncoding("utf8");child.stdout.on("data",onStdout);child.stderr.on("data",onStderr);child.stdin.on("error",onStdinError);child.once("error",onError);child.once("close",onClose);deadlineTimer=setTimeout(()=>{appendOutput(output.stderr,"comment-checker timed out",output,streamLimit,combinedLimit);abort()},deadline);child.stdin.end(stdin)})}async function runCommentChecker(input,options={}){const binaryPath=options.binaryPath??options.resolveBinary?.()??resolveCommentCheckerBinary();if(binaryPath===undefined){return{status:"missing",message:"comment-checker is not provisioned; set ASTERLINE_COMMENT_CHECKER_BINARY to an operator-provisioned executable."}}const args=options.customPrompt===undefined?["check"]:["check","--prompt",options.customPrompt];const result=await(options.executor??spawnProcess)(binaryPath,args,JSON.stringify(input),MAX_PROCESS_OUTPUT_BYTES,configuredTimeoutMs());const message=result.stderr.length>0?result.stderr:result.stdout;if(result.exitCode===0)return{status:"pass",message:"",binaryPath,exitCode:0};if(result.exitCode===2)return{status:"warning",message,binaryPath,exitCode:2};return{status:"error",message,binaryPath,exitCode:result.exitCode}}function resolveCommentCheckerBinary(){const configured=process.env["ASTERLINE_COMMENT_CHECKER_BINARY"];if(configured!==undefined&&existsSync(configured))return configured;const binaryName=process.platform==="win32"?"comment-checker.exe":"comment-checker";try{const require2=createRequire(import.meta.url);const packagePath=require2.resolve("@code-yeongyu/comment-checker/package.json");const binaryPath=join(dirname(packagePath),"bin",binaryName);return existsSync(binaryPath)?binaryPath:undefined}catch(error){if(error instanceof Error)return;throw error}}function configuredTimeoutMs(){const raw=process.env["ASTERLINE_COMMENT_CHECKER_TIMEOUT_MS"];if(raw===undefined||!/^\d+$/.test(raw))return 30000;const value=Number(raw);return Number.isSafeInteger(value)&&value>=100&&value<=30000?value:30000}var MAX_STDIN_CHARS=1048576;var MAX_HOOK_FEEDBACK_CHARS=8000;function extractAsterlineCommentCheckRequests(raw){const event=normalizeAuggieToolEventFailOpen(raw);if(event===null||event.phase!=="post"||!isSucceeded(event.state))return[];switch(event.tool){case"save-file":return event.input.content.length===0?[]:[request(event.tool,"Write",event.input.path,{file_path:event.input.path,content:event.input.content})];case"str-replace-editor":return replaceRequests(event);case"apply_patch":return parseApplyPatchRequests(event.input.patch,event.tool);case"launch-process":return[];default:return assertNever(event)}}async function runCommentCheckerPostToolUse(raw,options={}){const event=normalizeAuggieToolEventFailOpen(raw);if(event===null||event.workspaceRoots.length===0)return"";const requests=extractAsterlineCommentCheckRequests(event.raw);if(requests.length===0)return"";const run=options.run??runCommentChecker;const warnings=[];for(const item of requests){const result=await run(toHookInput(item,{sessionId:event.conversationId,cwd:event.workspaceRoots[0]??""}));switch(result.status){case"pass":case"missing":case"error":break;case"warning":{const message=normalizeText(result.message);if(message.length>0)warnings.push({filePath:item.filePath,message});break}default:assertNever(result)}}if(warnings.length===0)return"";return JSON.stringify({decision:"block",reason:limitText(warnings.map(({filePath,message})=>`comment-checker found issues in ${filePath}:
${message}`).join(`

`))})}async function runAsterlineHookCli(){const input=await readStdin();if(input===undefined||input.trim().length===0)return;const output=await runCommentCheckerPostToolUse(input);if(output.length>0)processStdout.write(`${output}
`)}function replaceRequests(event){const edits=event.input.edits.filter((edit)=>edit.newText!==undefined&&edit.newText.length>0).map((edit)=>({old_string:edit.oldText??"",new_string:edit.newText??""}));if(edits.length===0)return[];const first=edits[0];if(edits.length===1&&first!==undefined){return[request(event.tool,"Edit",event.input.path,{file_path:event.input.path,old_string:first.old_string,new_string:first.new_string})]}return[request(event.tool,"MultiEdit",event.input.path,{file_path:event.input.path,edits})]}function request(sourceToolName,toolName,filePath,toolInput){return{sourceToolName,toolName,filePath,toolInput}}function isSucceeded(state){switch(state.kind){case"succeeded":return true;case"pending":case"failed":case"cancelled":case"unknown":return false;default:return assertNever(state)}}function normalizeText(value){return value.replace(/\r\n/g,`
`).replace(/\r/g,`
`).trim()}function limitText(text){if(text.length<=MAX_HOOK_FEEDBACK_CHARS)return text;const marker=`

[Truncated hook output to protect the Auggie context window.]`;return`${text.slice(0,MAX_HOOK_FEEDBACK_CHARS-marker.length).trimEnd()}${marker}`}function readStdin(){return new Promise((resolve,reject)=>{let data="";let withinLimit=true;processStdin.setEncoding("utf8");processStdin.on("data",(chunk)=>{if(!withinLimit)return;data+=chunk;if(data.length>MAX_STDIN_CHARS)withinLimit=false});processStdin.once("error",reject);processStdin.once("end",()=>resolve(withinLimit?data:undefined))})}function assertNever(value){throw new TypeError(`Unreachable variant: ${String(value)}`)}var[command,subcommand]=process.argv.slice(2);if(command==="hook"&&subcommand==="post-tool-use"){await runAsterlineHookCli()}else{process.stderr.write(`Usage: asterline-comment-guard hook post-tool-use
`);process.exitCode=2}
